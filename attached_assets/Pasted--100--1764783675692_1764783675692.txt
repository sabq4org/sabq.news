مساء النور يا علي 🌙
إيه نعم الفكرة ممكنة 100٪ ومناسبة جدًا لمشروع سبق الذكية 👌

أقدر ألخّص فكرتها في جملة واحدة:

تكتب النص في المحرر → يروح للـ AI → يرجع لك نفس النص مع تعليمات: هذي الكلمة Bold، هذي عنوان، هذي اقتباس… والمحرر يطبّقها تلقائيًا.  ￼

خلنا نمشيها step by step وبعدين أعطيك “Prompt جاهز” + شكل الـ JSON اللي المبرمج يركّب عليه.

⸻

1️⃣ الفكرة التقنية ببساطة

السيناريو:
	1.	المحرر يكتب نص الخبر في المحرر المتقدم (Rich Text Editor).
	2.	يضغط زر مثل:
👉 “تنسيق ذكي بالنقاط الغامقة (Bold)”
	3.	يتم إرسال النص إلى API مثل:
/api/ai/auto-format
	4.	الذكاء الاصطناعي يرجّع:
	•	إمّا نفس النص بصيغة Markdown (فيه الكلمات المهمة بين **).
	•	أو JSON يحدد:
	•	الكلمة
	•	مكانها في النص
	•	نوع التنسيق: bold, italic, heading, quote …
	5.	المحرر (Tiptap / Slate / أي محرر متقدم) يطبّق التنسيق أوتوماتيك.

بما إن مشروع سبق الذكية فيه أصلاً Smart Editor / AI Integration، فنحن بس نضيف له Action جديد اسمه مثلًا:
"auto_format" أو "highlight_keywords" ضمن نفس منظومة الـ AI.  ￼

⸻

2️⃣ Bunty (Prompt) جاهز للذكاء الاصطناعي – بصيغة عملية

📌 أولاً: الـ System Prompt (ثابت)

هذا تحطه كـ System message في الـ API:

أنت محرر صحفي آلي متخصص في الأخبار العربية.
مهمتك هي تحليل النصوص الإخبارية وتحديد الكلمات والعبارات الأهم التي يجب إبرازها بصريًا داخل المحرر (مثل الكلمات المفتاحية، الأسماء المهمة، الجهات، الدول، المناصب، العناوين الفرعية).

أعد لي استجابة بصيغة JSON فقط بدون أي نص آخر خارج JSON.

صيغة الـ JSON يجب أن تكون كالتالي بالضبط:

{
  "strategy": "conservative | moderate | aggressive",
  "formatted_text": "نفس النص مع استخدام Markdown للتنسيق",
  "highlights": [
    {
      "text": "النص الذي سيتم تنسيقه",
      "type": "bold | italic | underline | heading | quote | highlight",
      "reason": "سبب اختيار هذه الكلمة أو العبارة",
      "importance": 1
    }
  ]
}

التعليمات:
	•	استخدم Markdown لوضع الكلمات أو العبارات المهمة بين ** لتكون بالخط العريض داخل المحرر.
	•	لا تُبالغ في كثرة الكلمات الغامقة؛ ركّز على:
	•	أسماء الأشخاص البارزين
	•	أسماء الدول والجهات الرسمية
	•	الأرقام والإحصائيات المهمة
	•	العناوين الفرعية داخل المتن إن وجدت
	•	لا تُعد صياغة الخبر بشكل كامل، ركّز على التنسيق فقط.
	•	لا تستخدم أي لغة غير العربية في المحتوى نفسه.
	•	لا تضف مقدمة ولا خاتمة؛ فقط النتيجة حسب نموذج الـ JSON أعلاه.

⸻

🧾 ثانيًا: مثال User Prompt (اللي يجي من المحرر)

{
  "mode": "auto_format",
  "tone": "news",
  "rules": {
    "bold_names": true,
    "bold_numbers": true,
    "bold_institutions": true,
    "max_bold_per_paragraph": 4
  },
  "text": "أدخل هنا نص الخبر كما يكتبه المحرر في سبق..."
}


⸻

🧪 ثالثًا: مثال نتيجة من الذكاء الاصطناعي

استجابة نموذجية (الـ AI يرجع شيء مثل كذا):

{
  "strategy": "moderate",
  "formatted_text": "أعلنت **وزارة الصحة** اليوم عن تسجيل **150 حالة جديدة** من فيروس **كورونا** في **المملكة العربية السعودية**، مشيرةً إلى ارتفاع إجمالي الحالات إلى **250,000 حالة**.",
  "highlights": [
    {
      "text": "وزارة الصحة",
      "type": "bold",
      "reason": "جهة حكومية رئيسية في الخبر",
      "importance": 1
    },
    {
      "text": "150 حالة جديدة",
      "type": "bold",
      "reason": "رقم أساسي في الخبر",
      "importance": 1
    },
    {
      "text": "كورونا",
      "type": "bold",
      "reason": "موضوع الخبر الرئيسي",
      "importance": 1
    },
    {
      "text": "المملكة العربية السعودية",
      "type": "bold",
      "reason": "نطاق جغرافي مهم",
      "importance": 1
    },
    {
      "text": "250,000 حالة",
      "type": "bold",
      "reason": "الإجمالي التراكمي للحالات",
      "importance": 1
    }
  ]
}

في الواجهة، المبرمج ياخذ formatted_text ويغذّي فيه المحرر المتقدم (Tiptap / ProseMirror مثلاً) أو يحوّله من Markdown إلى الـ Rich Text Schema ويطبّق الـ bold على طول.

⸻

3️⃣ كيف نركّبه على المحرر المتقدم في “سبق الذكية”؟

🛠️ EndPoint مقترح في Next.js

داخل مشروعك (اللي أصلاً مبني على Next.js + TypeScript + AI Integration)  ￼
تقدر تضيف Route مثل:

// /app/api/ai/auto-format/route.ts
import { NextRequest, NextResponse } from "next/server";
import OpenAI from "openai";

const client = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });

export async function POST(req: NextRequest) {
  const body = await req.json();
  const { text, rules } = body;

  const systemPrompt = `... هنا تحط System Prompt اللي كتبناه فوق ...`;

  const completion = await client.responses.create({
    model: "gpt-4.1-mini",
    input: [
      {
        role: "system",
        content: systemPrompt
      },
      {
        role: "user",
        content: JSON.stringify({
          mode: "auto_format",
          rules,
          text
        })
      }
    ],
    response_format: { type: "json_object" }
  });

  const result = completion.output[0].content[0].text;

  return NextResponse.json(JSON.parse(result));
}

🧩 من جهة المحرر (Frontend)
	1.	المستخدم يكتب الخبر في المحرر.
	2.	عند الضغط على زر: “تنسيق ذكي (Bold)”:
	•	ترسل النص إلى /api/ai/auto-format.
	3.	تستقبل formatted_text (بـ Markdown).
	4.	تحوله من Markdown → Document Structure للمحرر.
	5.	يعرض للمحرر نفس النص لكن:
	•	الكلمات المهمة صارت غامقة
	•	لاحقًا تقدر تزيد:
	•	اقتباس
	•	عناوين فرعية
	•	إبراز اقتباسات

⸻

4️⃣ خيارات التطوير والـ Level اللي تقدر توصل له

ممكن تبدأ بـ 3 مراحل:

🎯 المرحلة 1 – أبسط نسخة (MVP)
	•	AI يرجّع فقط formatted_text بـ Markdown.
	•	المحرر يحوّل Markdown → Rich Text.
	•	يركّز على bold فقط.

🚀 المرحلة 2 – تنسيق أعمق
	•	إضافة:
	•	عناوين فرعية داخل المتن.
	•	اقتباسات (Quote block).
	•	إبراز أسماء الأشخاص والمؤسسات.

🧠 المرحلة 3 – محرر “مُوَجَّه بالذكاء الاصطناعي”
	•	يكون عندك Panel جانبي:
	•	يقترح:
	•	“خلي هذي الفقرة عنوان فرعي”
	•	“هذي الجملة تصلح اقتباس رئيسي”
	•	والمحرر يوافق أو يرفض بنقرة واحدة.

⸻

5️⃣ الخلاصة يا أبو محمد
	•	نعم: ممكن جدًا تخلي الذكاء الاصطناعي ينسّق النص تلقائيًا داخل المحرر المتقدم، ويختار كلمات معيّنة تكون بخط عريض.
	•	تقنيًا الموضوع عبارة عن:
	•	Prompt مضبوط ✅
	•	API بسيط ✅
	•	تكامل مع محرر Rich Text ✅
	•	أعطيتك:
	•	System Prompt جاهز
	•	هيكل JSON واضح
	•	مثال كود API في Next.js
	•	تقدر تقول للمبرمج عندك:
“أبغى زر في المحرر، يرسل النص لـ AI ويرجع لي نفس النص لكن متنسّق Bold حسب الـ Prompt هذا”.

لو حاب، في الرسالة الجاية أقدر أعدّل الـ Prompt علشان:
	•	يقلل أو يزيد عدد الكلمات الغامقة.
	•	يفرّق بين خبر عاجل وتحليل مطوّل.
	•	أو نخصصه فقط لنمط “سبق” التحريري.