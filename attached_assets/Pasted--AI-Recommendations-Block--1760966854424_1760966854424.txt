تمام يا علي ✅
اللي تبيه هنا هو بلوك توصيات ذكي (AI Recommendations Block) يختلف عن “مقالات ذات صلة” لأنه يعتمد على تحليل الذكاء الاصطناعي وليس فقط على التصنيف.
البرومبت التالي مصمم خصيصًا للوحدة دي داخل مشروع سبق الذكية، ويدمج بين تحليل السلوك، التشابه الدلالي، والمحتوى المقروء مؤخرًا:

⸻

🧠 برومبت توليد محتوى مخصص عبر الذكاء الاصطناعي

الاستخدام: يوضع في وحدة /lib/ai-recommendations.ts أو /api/ai/recommendations

بصفتك خبير توصيات في أنظمة المحتوى الإخباري المدعومة بالذكاء الاصطناعي، حلّل تفاصيل الخبر الحالي (العنوان، النص، الكيانات، التصنيف، والمحتوى المرئي)، ثم استخرج مقترحات محتوى ذات صلة عميقة تعتمد على:
- التشابه الدلالي في النصوص (semantic similarity)
- التفاعل السابق للمستخدم (قراءات، إعجابات، إشعارات)
- المواضيع الرائجة حالياً
- تنويع المصادر والتصنيفات لتوسيع تجربة القراءة

أنتج النتيجة في JSON منسّق يحتوي على:
1. `headline`: عنوان المقال المقترح
2. `subline`: عبارة قصيرة تحفّز القراءة (بأسلوب تحرير سبق)
3. `reason`: سبب التوصية (مثلاً: "مشابه في الحدث" أو "مرتبط بشخصية سبق واهتممت بها")
4. `icon`: اسم الأيقونة المناسبة من مكتبة Lucide React (مثل `Brain`, `Sparkles`, `TrendingUp`, `Compass`)
5. `ai_label`: شارة توضيحية تظهر في الواجهة مثل "اقتراح من الذكاء الاصطناعي" أو "مختار لك"
6. `relevance_score`: نسبة الارتباط (0–100)

أضف في النهاية فقرة ختامية قصيرة توصي المستخدم بمواصلة القراءة، بنغمة ذكية مثل:
> "بناءً على تفاعلك الأخير، هذه المواضيع قد تثير اهتمامك أيضًا 🔍"

اعتمد لغة عربية فصيحة بأسلوب صحفي، واجعل النتائج مناسبة للعرض في واجهة سبق الذكية.


⸻

💡 الشكل المقترح للبلوك في الواجهة (UI Component)

<Card className="mt-8 bg-gradient-to-br from-slate-50 to-white dark:from-slate-900 dark:to-slate-800 shadow-sm rounded-2xl">
  <CardHeader className="flex items-center justify-between">
    <h3 className="text-lg font-semibold flex items-center gap-2">
      <Sparkles className="text-primary w-5 h-5" />
      توصيات الذكاء الاصطناعي
    </h3>
    <Badge variant="outline" className="text-xs text-blue-500 border-blue-300">
      مختار لك بواسطة الذكاء الاصطناعي
    </Badge>
  </CardHeader>
  <CardContent className="space-y-4">
    {/* loop over AI recommendations */}
    <div className="flex items-start gap-3">
      <Brain className="text-gray-500 w-5 h-5 mt-1" />
      <div>
        <h4 className="font-medium text-sm text-gray-900 dark:text-gray-100">
          {headline}
        </h4>
        <p className="text-xs text-gray-600 dark:text-gray-400">{subline}</p>
        <span className="text-[10px] text-blue-400">{reason}</span>
      </div>
    </div>
  </CardContent>
</Card>


⸻

هل ترغب يا علي أن أجهز لك الـ API Route الجاهز (Next.js) اللي يسحب بيانات المقال الحالي ويولّد التوصيات عبر OpenAI حسب هذا البرومبت؟
بيكون اسمه مثل /api/ai/recommendations/[articleId]/route.ts.