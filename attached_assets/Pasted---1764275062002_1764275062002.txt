تمام يا أبو محمد… ولا يهمك.
خلّنا نبني واجهة الرقابة الذكية للتعليقات داخل سبق الذكية—واجهة كاملة، حديثة، Tailwind + Radix + Lucide + Next.js، ومتوافقة تماماً مع الـUX/UI اللي تعتمدونه في المنصة.
وبأسلوب مختصر/وافي وعملي زي ما تحب.

⸻

🎛️ النتيجة: تصميم واجهة الرقابة الذكية للتعليقات (AI Moderation Dashboard)

واجهة متكاملة تشغّل نظام التعليقات بالذكاء الاصطناعي… واضحة، سريعة، متجاوبة، وتشتغل مباشرة داخل /admin/comments/moderation.

⸻

🌐 URL المقترح داخل لوحة التحكم

/admin/comments/moderation


⸻

🧩 مكوّنات الواجهة الأساسية

1) الشريط العلوي (Top Header)
	•	عنوان: الرقابة الذكية للتعليقات
	•	زر تحديث
	•	قائمة فلترة هندسية:
	•	الكل
	•	آمن (Safe)
	•	يحتاج مراجعة (Review)
	•	مرفوض (Rejected)
	•	شريط بحث سريع (للبحث بالاسم، البريد، ID، نص التعليق)

⸻

2) لوحة الإحصائيات (AI Metrics Cards)

4 بطاقات صغيرة — تعرض أداء الذكاء الاصطناعي:
	•	✔️ التعليقات المقبولة اليوم
	•	⚠️ التعليقات التي تحتاج مراجعة
	•	❌ التعليقات المرفوضة
	•	📈 متوسط دقة الذكاء الاصطناعي اليوم (AI Accuracy)

بطاقات بلون Radix UI + Tailwind gradients.

⸻

3) جدول التعليقات (Interactive Smart Table)

الأعمدة:
	•	المستخدم (Avatar + اسم)
	•	نص التعليق (مع قص 2 سطور)
	•	التصنيف:
	•	Safe (أخضر)
	•	Review (أصفر)
	•	Reject (أحمر)
	•	Score (شريط ملوّن)
	•	أسباب الرفض (Badges)
	•	التاريخ
	•	الإجراءات (Actions)

الميزات:
	•	فرز متقدم
	•	Infinite Scroll
	•	Quick Preview
	•	Highlighting للكلمات المسيئة تلقائيًا

⸻

4) نافذة التشغيل السريع (AI Auto-Decision Drawer)

عند الضغط على تعليق → تفتح لوحة جانبية فيها:

المحتوى:
	•	النص كامل
	•	تحليل الذكاء الاصطناعي (من البرومبت السابق)
	•	قائمة المشاكل المكتشفة عبر Badges:
	•	Toxicity
	•	Hate Speech
	•	Spam
	•	Threats
	•	Harassment

الأزرار:
	•	اعتماد التعليق
	•	رفض التعليق
	•	إرسال للتحرير البشري
	•	إعادة تحليل الذكاء الاصطناعي (Re-run AI)

⸻

5) فلترة متقدمة (AI Filters Drawer)

تدعم الفلترة حسب:
	•	نوع الضرر (toxicity / spam / threats…)
	•	درجة الخطورة (0–100)
	•	مستخدم محدد
	•	IP محدد
	•	مقال محدد
	•	وقت معيّن

⸻

🎨 تصميم Tailwind + Radix UI جاهز (كود قابل للاستخدام)

🔹 Header + Filters

<div className="flex items-center justify-between mb-6">
  <h1 className="text-2xl font-bold">الرقابة الذكية للتعليقات</h1>

  <div className="flex gap-2">
    <Button variant="outline">الكل</Button>
    <Button variant="secondary">آمن</Button>
    <Button variant="secondary">مراجعة</Button>
    <Button variant="destructive">مرفوض</Button>
  </div>
</div>


⸻

🔹 AI Score Badge

const ScoreBadge = ({ score }) => {
  let color =
    score >= 80 ? "bg-green-500" :
    score >= 40 ? "bg-yellow-500" :
    "bg-red-500";

  return (
    <span className={`px-3 py-1 rounded text-white text-xs ${color}`}>
      {score} / 100
    </span>
  );
};


⸻

🔹 تصنيفات AI (Safe / Review / Reject)

const StatusChip = ({ type }) => {
  const styles = {
    safe: "bg-green-100 text-green-700",
    review: "bg-yellow-100 text-yellow-700",
    reject: "bg-red-100 text-red-700",
  };

  return (
    <span className={`px-2 py-1 rounded text-xs ${styles[type]}`}>
      {type === "safe" ? "آمن" : type === "review" ? "مراجعة" : "مرفوض"}
    </span>
  );
};


⸻

🔹 Highlight الكلمات المسيئة داخل التعليق

function highlightBadWords(text, badWords) {
  let pattern = new RegExp(`(${badWords.join("|")})`, "gi");
  return text.replace(pattern, `<mark class="bg-red-200">$1</mark>`);
}


⸻

🧠 تكامل الذكاء الاصطناعي (Backend Logic)

واجهة الـAPI موجودة في المشروع حسب التوثيق:
/app/api/moderation/analyze/route.ts
وتعيد:

{
  "score": 92,
  "classification": "safe",
  "detected": [],
  "reason": "No harmful content detected"
}

والواجهة تتعامل معها هكذا:

async function analyzeComment(text) {
  const res = await fetch("/api/moderation/analyze", {
    method: "POST",
    body: JSON.stringify({ comment: text }),
  });
  return await res.json();
}


⸻

🧩 كيف يرتبط ذلك بجدول التعليقات؟

من التوثيق:
جدول comments يحتوي:
	•	ai_moderation_score
	•	ai_classification
￼

وعند اعتماد أو رفض تعليق:

await prisma.comment.update({
  where: { id },
  data: {
    is_approved: true/false,
    ai_classification: result.classification,
    ai_moderation_score: result.score
  }
});


⸻

🎯 هل تريد الخطوة التالية يا أبو محمد؟

أقدر الآن أجهّز لك:

✔️ كود كامل صفحة React/Next.js جاهز للنسخ

✔️ تصميم Figma كامل جاهز للتطبيق

✔️ Workflow كامل يربط الذكاء الاصطناعي باللوحة

✔️ كود API كامل يضبط المودريشن أوتوماتيكياً

إذا تبيني أبدأ…
اختر واحد من الثلاثة التالية:
	1.	“ابنِ لي الواجهة كاملة بالكود”
	2.	“أبغى تصميم Figma جاهز”
	3.	“أبغى API Workflow كامل ومُدمج”

قول لي رقم الخيار… وأنا أشتغل لك فورًا.