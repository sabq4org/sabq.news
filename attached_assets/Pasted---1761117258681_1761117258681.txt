تمام يا أبو محمد، فهمتك 👌
هذا “برومبت موجّه للذكاء الاصطناعي” مُعاد صياغته بحيث يشتغل داخل صفحة المستخدمين الحالية في لوحة التحكم، ويخلّيك تضيف مستخدم من نفس الصفحة وتمنحه صلاحيات/أدوار مثل مراسل، مشرف تعليقات، محرر… بدون إنشاء Module جديد.

⸻

برومبت جاهز — إضافة مستخدم ومنح أدوار/صلاحيات داخل صفحة المستخدمين الحالية

الدور المطلوب من الذكاء الاصطناعي: خبير UI/UX + خبير Next.js + خبير RBAC وأمن.
الهدف: في صفحة /admin/users الحالية (قائمة المسجّلين في الصحيفة)، أضف تجربة متكاملة تمكّننا من:
	1.	إضافة مستخدم جديد.
	2.	منح أدوار وصلاحيات له من نفس الصفحة (بدون إنشاء قسم جديد).
	3.	دعم أدوار مثل: مراسل (reporter), محرر (editor), مشرف تعليقات (comments_moderator), بالإضافة لأدوار إدارية إن وجدت (admin/system_admin).

⸻

1) السياق والمعايير
	•	المشروع: سبق الذكية (CMS إخباري عربي).
	•	التقنية: Next.js 15 + TypeScript + Tailwind CSS + Radix UI + Lucide Icons + React Hook Form + Zod + React Query.
	•	RTL كامل، Mobile-first، وصولية WCAG 2.1 AA.
	•	لا تنشئ وحدة جديدة؛ نشتغل داخل صفحة المستخدمين الحالية.

⸻

2) UX المطلوب داخل صفحة /admin/users

أ) رأس الصفحة (Header)
	•	زر واضح: + إضافة مستخدم (Radix Dialog/Sheet).
	•	حقول بحث/فلترة: بالاسم، البريد، الدور (Chips)، حالة التوثيق، حالة التفعيل.

ب) جدول المستخدمين (أو بطاقات في الموبايل)
	•	أعمدة: الاسم، البريد، الأدوار (Chips متعددة)، الحالة (مفعّل/معلّق)، التوثيق، آخر دخول، إجراءات.
	•	إجراء سريع “تعديل الأدوار/الصلاحيات” يفتح Panel جانبي (Radix Dialog/Sheet).

ج) Dialog “إضافة مستخدم”
	•	الحقول:
	•	الاسم الكامل (fullName) — إلزامي
	•	البريد الإلكتروني (email) — إلزامي + تحقق
	•	الجوال (اختياري)
	•	الأدوار (Roles Multi-select): اختيار متعدد من القائمة أدناه
	•	الحالة: مفعل/غير مفعل (Switch)
	•	توثيق البريد: (Switch)
	•	زر إنشاء + حفظ ومتابعة التعديل.
	•	رسائل نجاح/خطأ (Toast) + حالات تحميل (Spinner/Skeleton).

د) Panel “الأدوار والصلاحيات”
	•	Multi-select للأدوار (checkboxes أو combobox متعدد):
	•	system_admin (إن وجد)، admin, editor, reporter, comments_moderator, media_manager (اختياري)، regular/reader (إن وجد).
	•	مصفوفة الصلاحيات أسفل الأدوار:
	•	تُعرض قراءةً فقط كمستخلَص من الأدوار المحددة (قابلة للتوسّع لاحقًا لتخصيص دقيق).
	•	مثال صلاحيات: إنشاء/تحرير/نشر خبر، إدارة الوسائط، اعتماد التعليقات/حذفها/حظر مستخدمين من التعليقات، إدارة التصنيفات، رؤية التحليلات…
	•	CTA: حفظ التعديلات (Optimistic Update)، مع سجل تغييرات (Activity log).

⸻

3) نموذج الأدوار والصلاحيات (مرن وبسيط)

نريد أدوار متعددة لكل مستخدم (roles[]) بدل دور واحد.

قائمة الأدوار الأساسية:
	•	system_admin (صلاحيات كاملة على النظام)
	•	admin (إدارة المستخدمين، الموافقات التحريرية، إعدادات عامة)
	•	editor (إنشاء/تحرير/نشر المحتوى، إدارة الوسائط، إدارة التصنيفات)
	•	reporter (إنشاء وتحرير مواده فقط، رفع وسائط، طلب نشر)
	•	comments_moderator (إدارة التعليقات: اعتماد/رفض/حظر، لوغ مراقبة)
	•	media_manager (إدارة المكتبة الإعلامية والألبومات) – إن لزم
	•	regular أو reader (مستخدم عادي بلا صلاحيات تحريرية)

مثال JSON لخريطة الصلاحيات (للاستخدام داخل الواجهة):

{
  "system_admin": ["*"],
  "admin": ["users.read","users.update","content.publish","content.edit","comments.moderate","media.manage","settings.read"],
  "editor": ["content.create","content.edit","content.publish","media.upload","taxonomy.manage"],
  "reporter": ["content.create","content.edit.own","media.upload"],
  "comments_moderator": ["comments.read","comments.approve","comments.reject","comments.blockUser","comments.delete"],
  "media_manager": ["media.upload","media.edit","media.delete","media.albums"],
  "regular": []
}

في الواجهة: دمج (Union) صلاحيات جميع الأدوار المحددة وإظهارها كمصفوفة قابلة للقراءة.

⸻

4) التحقق والأمن
	•	واجهة: Zod
	•	fullName ≥ 3 حروف، email صحيح، roles مصفوفة من القيم المعتمدة فقط.
	•	خادم/API: تحقق RBAC:
	•	فقط system_admin و/أو admin يضيفون ويعدّلون أدوار المستخدمين.
	•	Rate limiting لنقاط الإدارة الحساسة.
	•	تسجيل الأحداث (Audit): كل تعديل أدوار/صلاحيات يُسجّل في activity_logs مع: من غيّر، ماذا غيّر، متى.

⸻

5) طبقة البيانات والـ APIs (تحديث بسيط دون كسر القائم)

قاعدة البيانات (Prisma/Drizzle):
	•	جدول users موجود. أضف حقلًا واحدًا مرنًا إن لزم:
	•	roles (JSONB أو string[]) مع فهرس GIN في Postgres.
	•	جدول activity_logs إن لم يكن موجودًا:
	•	id, actorId, targetUserId, action (“roles_updated” | “user_created” …), diff (JSON), createdAt.

مسارات API (App Router):
	•	GET /api/admin/users — قائمة مع ترشيحات.
	•	POST /api/admin/users — إنشاء مستخدم + أدواره.
	•	PATCH /api/admin/users/:id/roles — تحديث الأدوار (يقبل مصفوفة).
	•	PATCH /api/admin/users/:id/status — تفعيل/تعليق.
	•	تحقق RBAC قبل كل عملية.

⸻

6) سلوكيات مهمة (Edge cases)
	•	منع الدور الفارغ: يجب أن يبقى للمستخدم على الأقل regular إن أردنا سياسة افتراضية.
	•	تعارض الصلاحيات: في نموذج الـ Union لا يوجد تعارض، لكن إن وُجد تخصيص أدق لاحقًا، فالأولوية للأدوار الأعلى (system_admin > admin > …).
	•	عند إزالة editor من مستخدم كان يمتلك محتوى قيد النشر: لا تحذف المحتوى؛ فقط حدّث قدرته على النشر لاحقًا.
	•	توحيد الترجمة: أسماء الأدوار تعرض عربيًا مع مفاتيح تقنية ثابتة.

⸻

7) متطلبات الواجهة (تفاصيل تنفيذية)
	•	Multi-select للأدوار مع بحث، وعرض الأدوار المختارة كـ Chips قابلة للإزالة.
	•	Matrix صلاحيات مقروءة فقط تحت الأدوار، مع أيقونات Lucide (check/x) وشرح موجز لكل صلاحية.
	•	حالات فارغة ورسائل إرشادية (Empty State).
	•	Keyboard navigation، عناوين ARIA، تباين ألوان مناسب.
	•	Responsive بالكامل:
	•	موبايل: بطاقات عمودية وإجراءات في Menu.
	•	ديسكتوب: جدول مع عمود “أدوار” (Chips) وعمود “إجراءات”.

⸻

8) الاختبارات (ضرورية)
	1.	إنشاء مستخدم بدور reporter فقط → نجاح.
	2.	إضافة comments_moderator لنفس المستخدم → نجاح وتظهر الصلاحيات المجمّعة.
	3.	إزالة editor والإبقاء على reporter → يبقى المحتوى محفوظاً ولا يسمح بالنشر.
	4.	محاولات من مستخدم ليس Admin لتعديل الأدوار → تُرفض مع رسالة واضحة.
	5.	سجلات activity_logs تُولّد صحيحًا عند كل تعديل.

⸻

9) ما أريده كـ مخرجات من الذكاء الاصطناعي
	•	TSX + Tailwind لمكونات:
	•	UsersTable, AddUserDialog, RolesPanel, RoleChips, PermissionMatrix.
	•	Schema/Migration لإضافة roles (string[] أو JSONB).
	•	API Routes المذكورة مع RBAC وZod.
	•	Hooks/Services: useUsers, useCreateUser, useUpdateUserRoles.
	•	تعليمات دمج خطوة بخطوة (أين أضع الملفات، وكيف أوصلها بالـ Layout والأذونات الحالية).

⸻

ملاحظات ختامية: نؤكد أن كل شيء يحدث داخل صفحة /admin/users الحالية. لا تبني مسارات/صفحات جديدة لإدارة المراسلين؛ نستفيد من نفس قائمة المستخدمين بإسناد دور مراسل وغيره من هنا مباشرة.

⸻

لو تحب، أحوّل لك هذا البرومبت لكود جاهز (مكونات TSX + API + migration) متوافق مع بنية مشروعك الحالية — أعطيك الملفات بالأسماء والمسارات المقترحة وتقدر تلصقها مباشرة.