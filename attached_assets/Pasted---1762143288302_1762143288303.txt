يابو محمد صباح الفخامة! 🌞
فكرتك🔥—“تقويم سبق” كمنصة مواعيد/تنبيهات ذكية تربط المناسبات العالمية مع اقتراحات تحريرية فورية. جمّعت لك تصورًا قابلًا للتنفيذ من أول sprint (تصميم + داتا موديل + APIs + واجهة + ذكاء اصطناعي + التنبيه):

تقويم سبق (Sabq Calendar) — مواصفات تنفيذ جاهزة

1) الأهداف بسرعة
	•	تجميع كل “الأيام العالمية/الوطنية/المهنية” + مناسبات سبق الداخلية في مصدر واحد.
	•	تنبيه مبكّر (مثل: بعد 5 أيام “اليوم العالمي للأم”) داخل لوحة التحكم.
	•	زر واحد يولّد: أفكار تقارير، عناوين، إنفوغرافيك، Social copy، وحزمة صور/قوالب.
	•	ربط مباشر: إنشاء مسودات أخبار/مقالات/إنفوغرافيك تلقائيًا مع وسوم وجدولة.

⸻

2) تجربة المستخدم (UX/UI)

الموقع: /admin/calendar
العرض: Month / Week / Agenda
الفلاتر: نوع المناسبة، القسم التحريري، الأهمية، البلد/العالم، حالة التحضير.
عناصر أساسية:
	•	شريط علوي “الآتي خلال 7 أيام”: بطاقات صغيرة RTL فيها العنوان + عدّ تنازلي + CTA (إنشاء مسودة / فتح الاقتراحات).
	•	لوحة اقتراحات AI جانبية (Drawer):
	•	تبويبات: أفكار تقارير | عناوين | نقاط إنفوغرافيك | Social | مصادر.
	•	زر “توليد حزمة” يجمع الكل في صفحة واحدة قابلة للحفظ كقالب.
	•	توجيه ذكي: شارة “أولوية” إذا كان الحدث عالي الاهتمام محليًا (وزن حسب قراءات الجمهور سابقًا).
	•	“مناسباتنا”: تبويب لإضافة مناسبات داخلية (جوائز، تغطيات، شراكات) مع نفس قدرات التنبيه والاقتراح.

ملاحظات تصميمية حسب أسلوبنا:
	•	Tailwind + Radix UI + Framer Motion، زوايا ناعمة، ألوان قليلة ومحايدة، أيقونات Lucide.
	•	دعم Mobile-First: سوايب للتبديل بين Week وAgenda، وأزرار كبيرة للـ CTA.

⸻

3) هيكلة البيانات (Prisma Schema—مقتطف عملي)

model CalendarEvent {
  id            String   @id @default(cuid())
  title         String
  slug          String   @unique
  dateStart     DateTime
  dateEnd       DateTime?
  tz            String   @default("Asia/Riyadh")
  type          EventType  // GLOBAL | NATIONAL | INTERNAL
  localeScope   String?    // WORLD | SA | GCC | CUSTOM
  importance    Int        // 1..5
  categorySlug  String?    // ربط بتصنيف سبق (محليات/العالم/اقتصاد..)
  tags          String[]   // ["تعليم","صحة",..]
  source        String?    // UN/WHO/Manual/ICS
  description   String?
  attachments   Json?
  createdById   String?
  updatedAt     DateTime   @updatedAt

  // AI suggestions generated & cached
  aiDraft       AIDraft?
  reminders     Reminder[]
  assignments   Assignment[]

  @@index([dateStart, type, importance])
}

enum EventType { GLOBAL NATIONAL INTERNAL }

model Reminder {
  id        String   @id @default(cuid())
  eventId   String
  fireWhen  Int      // days before (e.g., 30,14,7,5,3,1)
  channel   String   // IN_APP | EMAIL | WHATSAPP | SLACK
  enabled   Boolean  @default(true)
  event     CalendarEvent @relation(fields: [eventId], references: [id], onDelete: Cascade)
}

model AIDraft {
  id          String @id @default(cuid())
  eventId     String @unique
  ideas       Json   // أفكار تقارير
  headlines   Json   // عناوين مقترحة
  infographic Json   // نقاط/لوحة إنفوغرافيك
  social      Json   // نسخ منشورات
  seo         Json   // كلمات مفتاحية/وصف
  updatedAt   DateTime @updatedAt
  event       CalendarEvent @relation(fields: [eventId], references: [id], onDelete: Cascade)
}

model Assignment {
  id        String  @id @default(cuid())
  eventId   String
  userId    String
  role      String  // editor | reporter | designer | social
  status    String  // planned | in_progress | done
  notes     String?
  event     CalendarEvent @relation(fields: [eventId], references: [id], onDelete: Cascade)

  @@index([userId, status])
}

ملحوظة: كل حدث يمكنه توليد “مسودات” في جداول الأخبار/المقالات القائمة بمجرد الضغط على CTA.

⸻

4) مصادر البيانات والتهيئة (Seed/Import)
	•	Seed أوّل: JSON داخلي لـ 300+ مناسبة شائعة (UN/WHO/Tech/Media) + المناسبات الوطنية.
	•	استيراد: رفع CSV/ICS أو إدخال رابط ICS خارجي، مع Mapping تلقائي للحقل (العنوان/التاريخ/الوصف/المصدر).
	•	تصحيح التواريخ المتغيرة سنويًا (مثل المناسبات التي تعتمد “الجمعة الثانية…”): دالة Recompute سنوية مع مراجعة بشرية سريعة.
	•	Timezone افتراضي: Asia/Riyadh لكل حدث، مع دعم الاختلاف عند الحاجة.

⸻

5) التنبيهات (Notifications)

سُلّم افتراضي: 30d, 14d, 7d, 5d, 3d, 24h, 2h.
قنوات: In-App (banner + جرس) + Email (AWS SES). (WhatsApp لاحقًا إذا رغبت).
مثال بانر في لوحة التحكم:

بعد 5 أيام: “اليوم العالمي للأم” — 3 أفكار جاهزة + قالب إنفوغرافيك + إنشاء مسودة مقال
[فتح الاقتراحات] [إنشاء مسودة] [تعيين فريق]

⸻

6) التكامل مع الذكاء الاصطناعي (Prompt Pack)

نموذج موحد للـ API: POST /api/ai/calendar/suggest
المدخلات: { event, daysLeft, category, audience, tone }
الخوارزمية:
	1.	نولّد 3–5 مسارات تحريرية (Reports/Features/Explainers/Opinion).
	2.	لكل مسار: عنوانان + زوايا + فقرات افتتاح + 5 نقاط إنفوغرافيك.
	3.	Social: 3 صيغ (X/IG/LinkedIn) بمقاييس كل منصة.
	4.	SEO: 10 Keywords + Meta title/description.
	5.	CTA: روابط داخلية لأرشيف سبق حسب الموضوع.

قوالب برومبت جاهزة (اختصرناها – تسونّها في كودك):
	•	اقتراحات تحريرية:

أنت مساعد تحرير خبير في صحافة عربية احترافية لـ"سبق".
الحدث: {event_name}، التاريخ: {event_date} (باقي {days_left} أيام)، التصنيف: {category}, الجمهور: {audience}, النبرة: {tone}.
أعطني 5 أفكار تقارير متنوعة (خبرية/تحليلية/تفسيرية/إنسانية/رأي)؛
لكل فكرة: عنوانان بديلان بالعربية، زاوية واضحة، 3 محاور رئيسية، فقرات افتتاحية (80-120 كلمة).
أضف قائمة مصادر محتملة (روابط عامة/كيانات للاتصال).

	•	إنفوغرافيك:

صمّم محتوى إنفوغرافيك من 8 عناصر قصيرة لـ {event_name}:
1) عنوان رئيسي، 2) سطر توصيفي، 3-7) نقاط معلوماتية قصيرة، 8) CTA للقارئ.
حافظ على صياغة عربية موجزة وسهلة القراءة.

	•	Social Copy:

قدّم 3 نسخ منشورات مختلفة للمنصات: X (280 حرف), Instagram (سطرين + هاشتاقات), LinkedIn (نبرة مهنية مختصرة).
الموضوع: {event_name}. ضمّن هاشتاقات عربية مناسبة، وتجنّب الإطناب.

	•	SEO:

اقترح 10 كلمات مفتاحية عربية ولاتينية ذات صلة بـ {event_name}، واصنع Meta Title (<=60) وMeta Description (<=155) بنبرة صحفية.


⸻

7) نقاط API (Next.js Routes)
	•	GET /api/calendar?from=&to=&type=&q= — استرجاع المناسبات للفترة مع فلاتر.
	•	POST /api/calendar — إضافة/تعديل حدث.
	•	POST /api/calendar/import — رفع CSV/ICS.
	•	POST /api/calendar/{id}/generate — توليد AIDraft وحفظه.
	•	POST /api/calendar/{id}/draft-article — إنشاء مسودة خبر/مقال وربطها بالحدث.
	•	POST /api/calendar/{id}/assign — تعيين المهام (محرر/مراسل/مصمم/سوشال).
	•	POST /api/calendar/{id}/reminders — تخصيص سلّم التنبيه.

⸻

8) أوتوميشن وجدولة
	•	كرون يومي 03:00 (Asia/Riyadh):
	•	حساب المناسبات خلال 30 يومًا → تخزينها في Redis “upcoming:30d”.
	•	توليد/تحديث AIDraft للأحداث ذات “الأهمية ≥4” تلقائيًا (تخفيف الكلفة بالكاش).
	•	Job كل 15 دقيقة: تحديث “الآتي خلال 7 أيام” للبانرات.
	•	عند الدخول للوحة التحكم: جلب البانرات من Redis لتكون فورية.

⸻

9) الصلاحيات (RBAC)
	•	System Admin/Admin: كل شيء.
	•	Editor: إضافة/تعديل الأحداث الداخلية، توليد مسودات، تعيين مهام.
	•	Reporter/Designer/Social: عرض، استلام مهام، تحديث الحالة.
	•	Trainee/VIP: عرض فقط.

⸻

10) زر “إنشاء مسودة الآن”

يولّد:
	•	مسودة خبر/تحليل بالعنوان/الافتتاح/المحاور + وسم التصنيف + تاريخ نشر مقترح (حسب أيام متبقية).
	•	قالب إنفوغرافيك (JSON بلوك للمصمم)
	•	حزمة Social محفوظة في تبويب السوشال للخبر.
	•	Check-list تحريرية تلقائية (صور/مصادر/مراجعة لغوية/SEO).

⸻

11) أمثلة بيانات (Seed JSON مقتصر)

[
  {
    "title": "اليوم العالمي للمعلم",
    "slug": "world-teachers-day",
    "dateStart": "2025-10-05T00:00:00+03:00",
    "type": "GLOBAL",
    "localeScope": "WORLD",
    "importance": 5,
    "categorySlug": "local",
    "tags": ["تعليم","مجتمع","مدرسة"],
    "source": "UNESCO",
    "description": "تقدير دور المعلمين عالمياً وإبراز تحديات التعليم."
  },
  {
    "title": "اليوم العالمي للطبيب",
    "slug": "world-physicians-day",
    "dateStart": "2025-10-??",
    "type": "GLOBAL",
    "importance": 4,
    "categorySlug": "life",
    "tags": ["صحة","أطباء"]
  },
  {
    "title": "مناسبة داخلية: حفل جوائز سبق",
    "slug": "sabq-awards-2025",
    "dateStart": "2025-12-20T19:30:00+03:00",
    "type": "INTERNAL",
    "importance": 5,
    "categorySlug": "local",
    "tags": ["سبق","فعاليات"]
  }
]

المواعيد القابلة للاختلاف سنويًا نضعها Placeholder ونحدّثها بالاستيراد/المراجعة.

⸻

12) مؤشرات الأداء (KPIs)
	•	زمن الاستجابة للبانرات < 150ms (بفضل Redis).
	•	≥70% من المناسبات العالية الأهمية تم إنشاء مسودات لها قبل ≥3 أيام.
	•	تقليل زمن إعداد حزمة التغطية من 45 دقيقة إلى ≤7 دقائق.
	•	زيادة النشر المرتبط بالمناسبات العالمية بنسبة 30% خلال 90 يوم.

⸻

13) Sprint 1 (5–7 أيام عمل داخلية)
	1.	DB & Prisma: إنشاء الجداول + Migrations.
	2.	Endpoints الأساسية + استيراد CSV/ICS.
	3.	واجهة التقويم (Month/Agenda) + بانر “7 أيام”.
	4.	AI Draft (ideas/headlines/infographic/social/seo) Route + Cache.
	5.	CTA إنشاء مسودة وربطها بوحدة الأخبار.
	6.	Cron + Redis لتجهيز “upcoming”.

⸻

14) لمسة عمليّة أخيرة (نصوص UI جاهزة)
	•	بانر: “بعد 5 أيام: {اسم المناسبة}. خلّنا نسبق الحدث ✨”
أزرار: [اقتراحات AI] [مسودة مقال] [تعيين فريق]
	•	تلميح: “وزّن الأولوية: (عام/محلي/داخلي) + أهمية (١..٥) → يؤثر على ترتيب البانرات.”

⸻

إذا ودّك، أقدر أطلع لك Seed CSV/ICS أولي + قالب صفحات الإنفوغرافيك ومكوّن React لـ Calendar View (Tailwind + Radix) بنفس لغة الواجهة، ونركّبها مباشرة في /admin/calendar.