أبو محمّد…
هذي أخيراً أعطتنا التشخيص الحقيقي 💥
والخبر الحلو؟ واضح جداً وما يحتاج تخمين.

خلّني أقولها لك كما هي:

⸻

🔥 المشكلة الفعلية الآن:

حقل الـ dueDate يتم تمريره على أنه “string عادية” أو “number” وليس Date Object

ومحرّك Drizzle يحاول يعمل:

value.toISOString()

لكن value ما هو Date
فـ Drizzle يطيح ويعطي:

TypeError: value.toISOString is not a function

هذا حرفياً السبب اللي يخلي:
	•	الإنشاء = يفشل
	•	قائمة المهام = ترجع صفر
	•	لكن العدادات = 7 (لأنها تحسب من جدول آخر أو من كاش مختلف)

⸻

🎯 السبب الجذري Root Cause:

الـ API تستقبل dueDate بهذا الشكل:

❌ مثال خاطئ من الواجهة:

"dueDate": "2025-11-15 17:52"

أو

"dueDate": 1731694320

أو حتى:

"dueDate": null

لكن Drizzle متوقع واحد من اثنين فقط:

✔️ String في شكل ISO صحيح

"dueDate": "2025-11-15T17:52:00.000Z"

✔️ أو Date object (من السيرفر)

new Date()


⸻

🔧 وش اللي لازم يتعدل؟ (الحل الجاهز والنهائي)

✅ الخطوة 1 — في الواجهة (React)

عدّل قبل إرسال الـ POST:

const formattedDueDate = dueDate ? new Date(dueDate).toISOString() : null;

await fetch("/api/tasks", {
  method: "POST",
  body: JSON.stringify({
    ...data,
    dueDate: formattedDueDate,
  }),
});


⸻

✅ الخطوة 2 — في API (Node / Express / Next)

احمِ نفسك:

const dueDate = body.dueDate ? new Date(body.dueDate) : null;

أو لضبط أكثر:

const dueDate = body.dueDate
  ? new Date(Date.parse(body.dueDate))
  : null;


⸻

🔬 المشكلة الثانية اللي ظهرت في اللوقز

لاحظ:

GET /api/tasks 304 :: {"tasks":[], "total":0}

هذا لأن:

المهام أصلاً ما تنحفظ بسبب خطأ الـ toISOString
وبالتالي القائمة فعلاً صفر.

يعني المشكلة مو من الفلاتر ولا من React Query.
هي بس بسبب فشل الإنشاء.

⸻

🎯 باختصار يا أبو محمد:

المشكلة:

❌ حقل الـ dueDate يرسَل غير ISO
❌ Drizzle يحاول يعمل له toISOString → ينهار
❌ POST يفشل 500
❌ GET يرجع صفر مهام
✔️ لكن العدادات قد تعتمد مصدر آخر (أو كاش) فتظهر 7

الحل:

✔️ تأكد أن dueDate دائمًا بصيغة ISO
✔️ ضيف تحويل date قبل الإرسال
✔️ ضيف parse في الـ API قبل الحفظ

⸻

⚡ جاهز أسوي لك كل شيء بنسخة جاهزة للنسخ الآن

تبغاني:
	•	أعطيك كود جاهز كامل لمعالجة التاريخ في الواجهة؟
	•	أو أعطيك نسخة كاملة للـ API تتعامل مع كل الاحتمالات؟
	•	أو تبغى تشخيص للحقول الثانية قبل ما تضيف مهام كثيرة؟

قول لي بس يا أبو محمد… وأنا أرتّبها لك على طول.