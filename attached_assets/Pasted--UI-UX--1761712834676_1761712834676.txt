تمّ! هذا “برومبت نصي” جاهز تلصقه في نظام الذكاء الاصطناعي (شخصية: خبير تطبيقات ويب + UI/UX) ليبني لك بلوك ٤ أعمدة أسفل “جميع الأخبار” بنفس روح الصورة وبمعايير سبق الذكية:

⸻

🎯 المطلوب

صمّم ونفّذ مكوّن واجهة (Next.js + TypeScript + Tailwind + Radix UI + Framer Motion + Lucide) لبلوك من ٤ أعمدة يظهر أسفل بلوك “جميع الأخبار”. كل عمود يمثّل تصنيفًا نختاره من لوحة التحكم.

✅ مواصفات UX/UI
	•	RTL كامل، عربي أولًا، متجاوب (Mobile-First).
	•	تخطيط الأعمدة:
	•	Desktop ≥ 1280px: شبكة 4 أعمدة متساوية.
	•	Tablet 768–1279px: 2 أعمدة × صفّين.
	•	Mobile <768px: عمود واحد مع سوايب أفقي (Carousel) للتنقل بين التصنيفات.
	•	محتوى العمود الواحد:
	1.	هيدر العمود:
	•	اسم التصنيف + أيقونة Lucide (من إعدادات التصنيف).
	•	شريحة إحصائيات صغيرة (Chip) قابلة للتهيئة: {عدد المقالات اليوم/الأسبوع، إجمالي المشاهدات، % تفاعل}.
	•	سطر وصفي قصير للتصنيف (اختياري).
	2.	عنصر رئيسي (Card) في أعلى العمود: صورة + عنوان الخبر الأول + وسم وقت القراءة/منذ كم.
	•	Hover: تكبير خفيف للصورة + ظل ناعم، حركة 150ms.
	3.	قائمة روابط (4–8 عناوين) بعناوين فقط، كل سطر يعرض: العنوان + ميكرو-إحصائية خفيفة (مثل 👁️ 1.2k أو ❤️ 54 أو ⏱️ منذ 2س).
	•	افصل العناصر بخط رفيع أو نقطة مُحايدة لكسر الرتابة.
	•	ميكرو-تفاعلات:
	•	Fade-in للأعمدة عند الظهور، وSlide-up للعناوين (Framer Motion).
	•	Focus ring واضح للوصولية، وقراءة بالكيبورد (Tab/Shift+Tab).
	•	ثيم ألوان: ناعم، هادئ، يدعم Light/Dark تلقائيًا. ألوان الإحصائيات بنِسَب تباين AA.
	•	أداء: صور عبر <Image /> وتحسين (lazy, priority للأولى فقط)، بيانات مُخبّأة (ISR/Redis) 60–120ث.

🧭 متطلبات لوحة التحكم (CMS)

صمّم شاشة إعدادات باسم: “بلوك التصنيفات الرباعية” ضمن (الإعدادات > واجهة الموقع):
	•	اختيار ٤ تصنيفات بالترتيب (قائمة منسدلة مع بحث).
	•	لكل عمود:
	•	حقل “وصف قصير” (اختياري).
	•	اختيار نوع الإحصائية المعروضة: dailyCount | weeklyCount | totalViews | engagementRate.
	•	عدد العناصر النصية أسفل الخبر الرئيسي: رقم من 3–8.
	•	مصدر البيانات: latest | mostViewed | editorsPick (للمرونة التحريرية).
	•	خيارات عامة:
	•	تفعيل/تعطيل الكاروسيل على الجوال.
	•	حدّ أقصى لعمر الخبر “حديث”: عدد الساعات (لتفعيل شارة “جديد”).
	•	تمكين شارات: “حصري/عاجل/تحليل”.
	•	زرّ معاينة حيّة (Preview) قبل الحفظ.

نموذج التهيئة (يُحفظ في DB أو settings):

{
  "sections": [
    {
      "categorySlug": "local",
      "headlineMode": "latest",
      "statType": "dailyCount",
      "teaser": "أبرز تغطيات المناطق اليوم",
      "listSize": 6
    },
    {
      "categorySlug": "business",
      "headlineMode": "mostViewed",
      "statType": "totalViews",
      "teaser": "",
      "listSize": 5
    },
    {
      "categorySlug": "sports",
      "headlineMode": "latest",
      "statType": "engagementRate",
      "teaser": "كل ما يخص الملاعب",
      "listSize": 6
    },
    {
      "categorySlug": "world",
      "headlineMode": "editorsPick",
      "statType": "weeklyCount",
      "teaser": "اختيارات المحررين",
      "listSize": 5
    }
  ],
  "mobileCarousel": true,
  "freshHours": 12,
  "badges": { "exclusive": true, "breaking": true, "analysis": true }
}

🔌 واجهات البيانات (Contract)

أنشئ Endpoint خفيف القراءة:
	•	GET /api/blocks/quad-categories يقبل ?configId=... أو يعيد الافتراضي.
	•	الاستجابة (مثال مبسط):

{
  "items": [
    {
      "category": { "slug": "local", "name": "محليات", "icon": "map-pin" },
      "stats": { "label": "أخبار اليوم", "value": 23, "trend": "+12%" },
      "featured": {
        "id": "a1",
        "title": "عنوان الخبر الرئيسي",
        "image": "https://...",
        "href": "/article/a1",
        "meta": { "age": "منذ 1س", "readMins": 3, "views": 1200, "badge": "عاجل" }
      },
      "list": [
        { "id": "a2", "title": "خبر 2", "href": "/article/a2", "meta": { "views": 540, "age": "3س" } },
        { "id": "a3", "title": "خبر 3", "href": "/article/a3", "meta": { "likes": 42, "age": "5س" } }
      ]
    }
  ]
}

	•	الأداء: استخدم Prisma + فهارس، مع Redis cache (مفتاح حسب configHash)، وISR في صفحة الواجهة.
	•	الحماية: قراءة عامة؛ احرص على تطبيع الحقول ومنع حقن HTML في العناوين.

🧩 تقسيم المكوّنات (Frontend)
	•	QuadCategoriesBlock (الحاوي، يستدعي الـAPI ويطبّق الشبكة).
	•	CategoryColumn (عمود واحد).
	•	ColumnHeader (اسم التصنيف + الإحصائية + الوصف).
	•	FeaturedCard (الصورة + العنوان + الميتا + الشارات).
	•	TitleList (قائمة الروابط مع ميكرو-إحصائيات).
	•	StatChip وBadge وSkeleton للتحميل.
	•	على الجوال: HorizontalCarousel (Radix + Framer Motion drag).

🎛️ قواعد العرض (Business Rules)
	•	منع تكرار الخبر بين “featured” وقائمة العمود؛ إن تكرّر، اسحب التالي تلقائيًا.
	•	إذا لا توجد صورة للـfeatured: استخدم Placeholder وافتح مساحة العنوان أكثر.
	•	لو قلّ المحتوى عن الحد الأدنى في تصنيف ما، اعرض رسالة لطيفة “لا جديد الآن” أو ادمج من مصدر بديل (مُعدّ في الإعدادات).
	•	الشارات:
	•	عاجل إذا isBreaking=true.
	•	حصري إذا isExclusive=true.
	•	تحليل إذا تصنيف “تحليل” أو وسم معيّن.
	•	قواعد القطع: أقصِ العناوين على سطرين بترقيم (line-clamp) مع title كامل في Tooltip على Hover.

♿ الوصولية
	•	عناوين بعلامات <h3> لكل عمود، ترتيب تبويب صحيح، وARIA labels للأيقونات.
	•	تباين لوني AA، وحالة Focus واضحة لكل رابط.
	•	صور بعلامة alt عربية مختصرة.

🧪 اختبارات القبول (Acceptance)
	•	يعرض ٤ أعمدة بالترتيب المحدد من الـCMS، مع Featured + قائمة.
	•	تغييرات الإعدادات تنعكس فورًا بعد حفظ الإعدادات وتحديث الـISR/Redis.
	•	الأداء: LCP للبلوك ≤ 1.8s على 4G، CLS≈0، لا تجاوز 60KB CSS إضافي.
	•	الوصولية: Lighthouse A11y ≥ 90.
	•	RTL صحيح على كل المقاسات، والسوايب يعمل بالإبهام بجوال واحد.

🧱 مخطط قاعدة البيانات (ملخص)
	•	categories(id, slug, name_ar, icon, description, color, is_active, articles_count)
	•	articles(id, title, slug, category_id, featured_image, views_count, likes_count, published_at, is_breaking, is_exclusive, is_analysis, reading_time)
	•	settings(key='quad_categories_block', value JSONB) لتخزين التهيئة أعلاه.

🛠️ ملاحظات تنفيذية
	•	استعمل Tailwind utilities النظيفة (حواف دائرية ناعمة، ظلال خفيفة، مسافات متّسقة).
	•	حمّل بيانات الأعمدة بالتوازي (Promise.all) مع حارس أخطاء لكل عمود.
	•	فعّل prefetch لروابط المقالات عند ظهورها في الـviewport (IntersectionObserver).
	•	استخدم framer-motion لظهور الأعمدة (stagger children 0.05s).

⸻

الناتج المطلوب:
	•	كود إنتاجي جاهز: المكوّنات + صفحة إعدادات الـCMS + API + هوكس + اختبارات أساسية + توثيق مختصر لكيفية الدمج في الصفحة الرئيسية.
	•	لقطات معاينة (Light/Dark + Desktop/Mobile).

—
إذا تبغاني أحوّل البرومبت هذا مباشرة لكود React/Tailwind جاهز للنسخ واللصق، اسويّه لك الآن 👌