تمام يا أبو محمد! جهزت لك برومبت تنفيذي مُوجَّه لوكيل ذكاء اصطناعي “خبير UI/UX وبرمجة” لتفعيل وحدات لوحة التحكم خطوة–بخطوة (تصنيفات، مستخدمون، أخبار، مقالات، صلاحيات، وإدارة الكيانات التحريرية). انسخه كما هو إلى وكيلك وخلّه ينفّذ 👇

⸻

🎯 برومبت مُوجَّه لوكيل الذكاء الاصطناعي (خبير UI/UX + برمجة)

أنت خبير في UI/UX حديث وبرمجة (Next.js + TypeScript + Prisma + PostgreSQL + Tailwind + Radix UI + shadcn/ui). المطلوب: تفعيل وإكمال وحدات لوحة التحكم التالية وفق أفضل معايير Sabq AI CMS، مع واجهات RTL عربية، دعم الأذونات (RBAC)، والسجلات (Audit Logs).

0) معايير عامة (واجبة)
	•	تصميم حديث: Tailwind + Radix + shadcn/ui، Mobile-First، تباين عالٍ، دعم لوحة مفاتيح، وARIA.
	•	تجربة إدارة متسقة: بنية صفحات /admin/<module> مع قوائم، جدول بيانات، نماذج Modal/Drawer، وشرائح مراجعة (Review Pane).
	•	حالات المحتوى: (draft, scheduled, published, archived), تواريخ النشر، الكاتب، التصنيف.
	•	RBAC: أدوار + صلاحيات دقيقة لكل إجراء (عرض/إضافة/تعديل/حذف/نشر/تعليق/حظر).
	•	سجل مراجعات: كل عملية CRUD تُسجَّل في activity_logs مع (old_value/new_value, actor).
	•	بحث + فرز + ترشيح: نصي + حسب الحالة + التاريخ + الكاتب + التصنيف، مع Pagination.
	•	سهولة الدمج: توليد أنواع TypeScript، Prisma schema, API routes، وصفحات الإدارة، وبذور بيانات (seed).
	•	اختبارات قبول: كتابة checklist لكل وحدة (مذكورة أسفل كل قسم).

⸻

1) إدارة التصنيفات (/admin/categories)

المهام
	•	عرض جدول التصنيفات الحالية (الاسم العربي، الاسم، الـslug، اللون، الأيقونة، الترتيب، الحالة).
	•	إنشاء/تعديل/حذف/تفعيل/تعطيل تصنيف.
	•	فحص فريدانية slug ومنع حذف تصنيف مرتبط بمحتوى إلا بخيار نقل المحتوى لتصنيف بديل.
	•	اختيار اللون (Color Picker) + أيقونة (Lucide) + ترتيب العرض.

API
	•	GET /api/categories?query=&is_active=&page=&limit=
	•	POST /api/categories (name_ar, name, slug, color, icon, is_active, display_order)
	•	PATCH /api/categories/:id
	•	DELETE /api/categories/:id?reassign_to=<catId>

Prisma (نموذج مُقترح)

model Category {
  id            Int      @id @default(autoincrement())
  name          String
  nameAr        String
  slug          String   @unique
  description   String?
  color         String   @default("#3B82F6")
  icon          String   @default("folder")
  isActive      Boolean  @default(true)
  displayOrder  Int?     
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  // relations
  articles      Article[]
  newsItems     News[]
}

قبول
	•	إنشاء/تعديل/حذف يعمل 100% مع تحقق slug.
	•	نقل محتوى تلقائيًا عند حذف تصنيف مستخدم.
	•	بحث/فرز/ترشيح + Pagination.

⸻

2) إدارة المستخدمين (/admin/users)

المهام
	•	عرض المستخدمين المسجَّلين عبر العضوية بحقول: الاسم، البريد، الحالة (مفعّل/غير مفعّل)، الدور، تاريخ الإنشاء، توثيق البريد.
	•	إجراءات: تفعيل/تعطيل، تعديل بيانات عضو، حذف عضو، تعليق/إيقاف مؤقت، حظر/رفع الحظر.
	•	تعديل الدور وربطه بمجموعات صلاحيات محددة.
	•	عرض أنشطة العضو (آخر دخول، منشورات مملوكة، تعليقات، إشارات نظام).

API
	•	GET /api/users?query=&role=&status=&page=&limit=
	•	PATCH /api/users/:id (name, email, role, status)
	•	POST /api/users/:id/suspend (reason, until?)
	•	POST /api/users/:id/ban (reason)
	•	DELETE /api/users/:id

حالات المستخدم
	•	active, inactive, suspended, banned, email_unverified

قبول
	•	تغيير الحالة والدور ينعكس فورًا.
	•	لا يُحذف مستخدم له محتوى إلا بخيارات نقل/أرشفة.
	•	سجل تدقيق لكل إجراء إداري.

⸻

3) إدارة الأخبار (/admin/news)

المجال: المحتوى الإخباري (خبَر).
المهام
	•	جدول شامل: العنوان، الحالة (مسودة/مجدول/منشور/مؤرشف)، تاريخ النشر، الكاتب، التصنيف، عدد القراءات/التعليقات.
	•	إنشاء/تعديل/حذف خبر، تحويل الحالة: مسودة ⇄ مجدول ⇄ منشور ⇄ مؤرشف.
	•	الحقول الرئيسية: العنوان، الـslug، الملخّص، المتن (Editor)، صورة بارزة، الكاتب (user/reporters)، التصنيف، الوسوم، تاريخ/ساعة النشر (يدوي/الآن)، سمات SEO، إتاحة كـ Featured.
	•	زر “تحويل إلى مسودة/نشر الآن/جدولة”.
	•	إظهار اسم الكاتب والتصنيف وتاريخ/وقت النشر في القائمة والبطاقة.

API
	•	GET /api/news?query=&status=&category=&author=&from=&to=
	•	POST /api/news (fields…)
	•	PATCH /api/news/:id
	•	DELETE /api/news/:id
	•	POST /api/news/:id/publish|schedule|archive|unpublish

Prisma (نموذج مُقترح)

enum ContentStatus { DRAFT SCHEDULED PUBLISHED ARCHIVED }

model News {
  id            String   @id @default(cuid())
  title         String
  slug          String   @unique
  excerpt       String?
  content       String
  featuredImage String?
  authorId      String
  categoryId    Int
  status        ContentStatus @default(DRAFT)
  publishedAt   DateTime?
  seo           Json?
  isFeatured    Boolean @default(false)
  viewsCount    Int     @default(0)
  commentsCount Int     @default(0)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  // relations
  author        User     @relation(fields: [authorId], references: [id])
  category      Category @relation(fields: [categoryId], references: [id])
  comments      Comment[]
}

قبول
	•	التحويل بين الحالات سليم مع ضوابط التاريخ.
	•	حفظ تلقائي للمسودة + تحذير ترك الصفحة دون حفظ.
	•	فحص ازدواجية slug، وتبنّي إعادة كتابة تلقائية.

⸻

4) إدارة المقالات (/admin/articles)

المجال: المقالات الرأي/الرأي العام.
مماثلة للأخبار مع اختلافات طفيفة:
	•	أنواع المقال: OPINION, ANALYSIS, COLUMN.
	•	سيرة الكاتب (بطاقة جانبية)، إمكانية تثبيت كاتب رأي.
	•	خط تدقيق تحريري (اختياري): مسودة → مراجعة → موافقة → نشر.

Prisma إضافة

enum ArticleType { OPINION ANALYSIS COLUMN }

model Article {
  id            String   @id @default(cuid())
  type          ArticleType @default(OPINION)
  title         String
  slug          String   @unique
  content       String
  excerpt       String?
  featuredImage String?
  authorId      String
  categoryId    Int
  status        ContentStatus @default(DRAFT)
  publishedAt   DateTime?
  seo           Json?
  isFeatured    Boolean @default(false)
  viewsCount    Int     @default(0)
  commentsCount Int     @default(0)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  author        User     @relation(fields: [authorId], references: [id])
  category      Category @relation(fields: [categoryId], references: [id])
  comments      Comment[]
}

قبول
	•	فصل واضح بين الأخبار والمقالات في المسارات والروابط والفلترة.
	•	عرض بطاقات “كتّاب الرأي” وإسناد سريع بالبحث.

⸻

5) إدارة الصلاحيات والأدوار (/admin/roles)

المهام
	•	تكوين أدوار (Roles) ومجموعات (Role Groups) وصلاحيات دقيقة (Permissions) عبر واجهة بديهية.
	•	أمثلة مجموعات: المحررون، مراجعو التعليقات، مشرفو النظام.
	•	صلاحيات دقيقة لكل وحدة:
	•	التصنيفات: view, create, update, delete
	•	الأخبار/المقالات: view, create, edit_own, edit_any, publish, unpublish, delete, archive, feature
	•	المستخدمون: view, create, update, delete, suspend, ban, change_role
	•	التعليقات: view, approve, reject, delete, restore
	•	النظام: manage_roles, manage_settings, view_audit_logs

Prisma (مقترح مبسّط)

model Role {
  id          String   @id @default(cuid())
  name        String   @unique
  description String?
  permissions Permission[]
  users       User[]
}

model Permission {
  id     String @id @default(cuid())
  code   String @unique // e.g., "news.publish"
  label  String
  roles  Role[]
}

model User {
  id       String @id @default(cuid())
  email    String @unique
  name     String
  roleId   String?
  role     Role?  @relation(fields: [roleId], references: [id])
  // ...
}

API
	•	GET /api/roles + GET /api/permissions
	•	POST /api/roles (name, description, permissionCodes[])
	•	PATCH /api/roles/:id + DELETE /api/roles/:id
	•	PATCH /api/users/:id/role (roleId)

قبول
	•	تعيين/سحب صلاحيات يعمل ويضبط ظهور الأزرار في الواجهة.
	•	منع إجراء غير مصرح به (backend + frontend guard).

⸻

6) إدارة الكيانات التحريرية (مراسلون/كتّاب/مشرفون) (/admin/staff)

كان اسمها عندكم “الإدارة/إدارة المراجعة” – نسمّيها هنا إدارة الكيانات التحريرية لِوَضوح الغرض.

المهام
	•	إضافة وإدارة: المراسلون، الكُتّاب، المشرفون (اسم، مسمى وظيفي، نبذة، صورة، تخصّصات، رابط لحساب المستخدم إن وُجد).
	•	ربطهم بمجموعات الصلاحيات المحددة سابقًا.
	•	إظهار الإحصاءات: عدد المواد المنشورة، المشاهدات، آخر نشاط.

API
	•	GET /api/staff?type=reporter|writer|supervisor
	•	POST /api/staff (fields…)
	•	PATCH /api/staff/:id + DELETE /api/staff/:id
	•	PATCH /api/staff/:id/assign-role (roleId)

قبول
	•	إنشاء سريع ببطاقة/Modal مع رفع صورة.
	•	ربط بحساب مستخدم أو إنشاؤه لحظيًا (اختياري).

⸻

7) التعليقات (/admin/comments) — لمركزية التعامل

المهام
	•	عرض تعليقات مع مرشحات: قيد المراجعة/مقبول/مرفوض/مُبلغ عنه.
	•	إجراءات: اعتماد/رفض/حذف/استعادة + سجل قرار.
	•	ربط صلاحيات “Moderate Comments” بالمجموعة المخصصة.

API (مختصر)
	•	GET /api/comments?status=&entity=news|article&from=&to=
	•	POST /api/comments/:id/approve|reject|delete|restore

قبول
	•	قرارات مجمّعة (Bulk), مع توثيق السبب في سجل المراجعة.

⸻

8) عناصر الواجهة المشتركة
	•	Tables: shadcn Data Table + Server-side pagination.
	•	Forms: React Hook Form + Zod validation.
	•	Toasts/Dialogs: Radix UI (Confirm before destructive).
	•	Badges: حالات المحتوى/المستخدم.
	•	Empty States: نصوص واضحة + CTA “إنشاء جديد”.
	•	Skeletons: تحميل سلس.
	•	i18n-RTL: اتجاه عربي افتراضي مع دعم لاحق لإنجليزي.

⸻

9) الأمن والتدقيق
	•	حماية المسارات الإدارية بـ RBAC، وجميع الأزرار تتحقق من الصلاحيات قبل العرض والتنفيذ.
	•	Activity Logs لكل إجراء (actorId, action, entityType, entityId, old/new, timestamp, ip, user_agent).
	•	Soft delete للمحتوى مع إمكانية الاستعادة حيث يلزم.

⸻

10) اختبارات القبول (مختصر تجميعي)
	•	التصنيفات: CRUD كامل + نقل محتوى عند الحذف + فريدانية slug.
	•	المستخدمون: تغيير حالة/دور، تعليق/حظر، منع حذف دون معالجة المحتوى.
	•	الأخبار/المقالات: حالات المحتوى + جدولة + SEO + slug + إحصاءات.
	•	الصلاحيات: منع أزرار/نهايات API لغير المصرح لهم.
	•	التعليقات: موافقة/رفض/حذف/Bulk + سجل قرار.
	•	الكيانات التحريرية: إضافة/ربط دور/إحصاءات.

⸻

11) مخرجات مطلوبة من الوكيل
	1.	توليد Prisma schema محدثة (النماذج أعلاه) + migrations.
	2.	Routes REST/Next API كاملة مع Zod validation + RBAC guards.
	3.	صفحات /admin/ لكل وحدة (Table + Filters + Modals + Forms).
	4.	seed.ts: أدوار افتراضية (system_admin, admin, editor, reporter, comments_mod) وصلاحياتها.
	5.	Playwright/Vitest: اختبارات smoke لوحدات CRUD والحالات الرئيسية.
	6.	Docs موجزة داخل README_ADMIN.md توضح التدفق والصلاحيات.

⸻

12) معايير UX الدقيقة
	•	أزرار الإجراءات الأساسية ثابتة أسفل/أعلى (Sticky Actions).
	•	تمييز بصري واضح للحالات (Badges: draft/scheduled/published/archived).
	•	Undo ممكن للحذف اللطيف (soft delete) لمدة 10 ثوانٍ.
	•	تنبيهات واضحة قبل حذف إتلافي + ذكر التأثير (مثال: نقل محتوى).
	•	دعم Keyboard shortcuts شائعة (⌘/Ctrl+S للحفظ، E للتحرير، / للبحث).
	•	Loading states واقعية (Skeleton + Busy indicators في الأزرار).

⸻

نفّذ كل ما سبق بدقة، وارجع بالمخرجات (Schema + API + صفحات الإدارة + seed + اختبارات) جاهزة للدمج.
أي تعارضات في البيانات: قدّم ترحيلات (migrations) وملف ترحيل بيانات (script) واضح.

⸻

إذا تبغاني أطلع لك نسخة “مضغوطة قصيرة” من البرومبت هذا عشان تلصقها في أداة ثانية، أجهزها لك فورًا ✅