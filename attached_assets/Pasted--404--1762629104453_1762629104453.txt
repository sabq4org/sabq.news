فكرة ممتازة يا علي 👌
صفحة 404 ما لازم تكون مملة أو “ميتة”. بالعكس، فرصة تخلي المستخدم يبتسم ويشعر إن حتى لما يضيع، هو داخل تجربة ذكية وممتعة.
بما أن مشروع سبق الذكية قائم على الذكاء الاصطناعي، خلينا نصمم صفحة فيها روح الدعابة + طابع تقني + لمسة “AI” واضحة.

إليك فكرة كاملة لصفحة 404 متوافقة مع معايير التصميم في المشروع (Next.js + Tailwind + Framer Motion):

⸻

🎨 الاقتراح

صفحة فيها:
	•	رسالة مرحة من الـ AI وكأن النظام يتحدث للمستخدم.
	•	حركة خفيفة (glitch effect أو typing animation).
	•	زر يعيده للصفحة الرئيسية أو “يدعه يسأل الذكاء الاصطناعي عن وجهته”.
	•	خلفية فيها مزيج من الألوان الداكنة والبنفسجية (رمز التقنية والخيال العلمي).

⸻

🧠 نموذج الكود

// /app/not-found.tsx
"use client";

import { motion } from "framer-motion";
import Link from "next/link";
import { BrainCircuit, Home, Sparkles } from "lucide-react";

export default function NotFoundPage() {
  return (
    <div className="min-h-screen flex flex-col items-center justify-center bg-gradient-to-br from-gray-900 via-purple-900 to-black text-white text-center px-6">
      <motion.div
        initial={{ opacity: 0, y: -20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.8 }}
        className="flex flex-col items-center space-y-4"
      >
        <BrainCircuit className="w-16 h-16 text-purple-400 animate-pulse" />
        <h1 className="text-5xl font-bold">404 - لم يتم العثور على الصفحة</h1>
        <motion.p
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 0.8 }}
          className="text-lg text-gray-300 max-w-md"
        >
          يبدو أنك دخلت في ثقب أسود من الإنترنت...  
          <span className="text-purple-400">لكن لا تقلق!</span>  
          الذكاء الاصطناعي يحاول الآن إعادة توجيهك.
        </motion.p>

        <div className="flex flex-col sm:flex-row gap-4 mt-8">
          <Link
            href="/"
            className="flex items-center gap-2 bg-purple-600 hover:bg-purple-700 text-white px-5 py-3 rounded-2xl transition-all"
          >
            <Home className="w-5 h-5" /> العودة إلى الرئيسية
          </Link>

          <Link
            href="/ai-assistant"
            className="flex items-center gap-2 border border-purple-400 hover:bg-purple-800 px-5 py-3 rounded-2xl text-purple-300 transition-all"
          >
            <Sparkles className="w-5 h-5" /> دع الـ AI يساعدك!
          </Link>
        </div>
      </motion.div>

      <motion.div
        animate={{ opacity: [0.3, 1, 0.3] }}
        transition={{ duration: 3, repeat: Infinity }}
        className="mt-16 text-sm text-gray-500"
      >
        🤖 <span className="text-purple-400">سبق الذكية</span> — حتى الصفحات المفقودة عندنا عندها شخصية.
      </motion.div>
    </div>
  );
}


⸻

💡 فكرة إضافية

يمكنك ربط زر “دع الـ AI يساعدك” بصفحة /ai-assistant أو نافذة محادثة صغيرة مع المساعد (مثل ChatGPT داخل سبق)، فيظهر كأن النظام يحاول “البحث عن وجهة المستخدم بنفسه”.

⸻

📱 النتيجة بصريًا
	•	شاشة داكنة أنيقة.
	•	أيقونة دماغ مضيئة.
	•	سطر نص ذكي ومضحك (“يبدو أنك دخلت في ثقب أسود من الإنترنت…”).
	•	زران متفاعلان بتأثيرات ناعمة.
	•	شعور أن الصفحة جزء من منظومة ذكية وليس خطأ تقني بحت.

⸻

هل ترغب يا علي أن أجهز نسخة متوافقة مع تصميم واجهة سبق الذكية (نمط الألوان والخطوط الرسمية للموقع)؟
أقدر أضبطها بحيث تتناسق 100٪ مع هوية المنصة.