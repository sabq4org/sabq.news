تمام علي 🙌 هذا برومبت موجه للذكاء الاصطناعي (خبير في UX/UI وTypeScript وNext.js) لتطوير صفحة إنشاء الخبر داخل مشروع سبق الذكية بشكل احترافي ومتكامل 👇

⸻

🧠 برومبت التطوير

أنت خبير في تطوير واجهات إدارة المحتوى (CMS) باستخدام Next.js 15 + TypeScript + Tailwind CSS + shadcn/ui + Radix UI.
نعمل ضمن مشروع “سبق الذكية” Sabq AI CMS الذي يحتوي على لوحة تحكم إخبارية ذكية.
المطلوب هو تطوير صفحة إنشاء خبر جديد (داخل /admin/news/create) وفق المعايير التالية:

⸻

🎨 المتطلبات الوظيفية والتصميمية
	1.	العنوان الفرعي:
	•	أضف حقل نصي جديد فوق حقل العنوان الرئيسي بعنوان العنوان الفرعي.
	•	يكون بطول متوسط (maxLength = 120).
	•	يعرض مؤشر عدد الأحرف المتبقية.
	2.	أنواع الخبر:
	•	أضف قسم اختيار نوع الخبر (نوع الخبر) باستخدام أزرار اختيار (Radio Group):
	•	🔴 خبر عاجل
	•	⭐ خبر مميز
	•	⚪ خبر عادي
	•	القيم تحفظ في حقل news_type داخل قاعدة البيانات.
	3.	النشر الفوري والمجدول:
	•	أضف مفتاح تبديل (Toggle) بعنوان نوع النشر.
	•	عند اختيار نشر مجدول، يظهر حقل لتحديد التاريخ والوقت (datetime-local).
	•	الحقول:

publish_type: "instant" | "scheduled";
scheduled_at?: Date;


	4.	قسم SEO (تحسين محركات البحث):
	•	أضف تبويب فرعي (Tab) بعنوان SEO يحتوي على:
	•	حقل meta_title (max 70).
	•	حقل meta_description (max 160).
	•	حقل slug (قابل للتوليد التلقائي من العنوان مع زر “توليد تلقائي”).
	•	معاينة فورية لكيف سيظهر في نتائج Google (snippet preview).
	•	استخدم مكون مخصص <SeoPreview /> يعرض:
	•	العنوان بخط أزرق
	•	الرابط باللون الأخضر
	•	الوصف الرمادي الفاتح
	5.	التصميم العام:
	•	اتبع تصميم واجهات سبق الذكية: حواف دائرية rounded-2xl، فراغات مريحة p-6، وتدرجات ناعمة.
	•	قسّم الصفحة إلى قسمين:
	•	اليسار (70%) → الحقول الرئيسية (العنوان، العنوان الفرعي، المحتوى، الصور)
	•	اليمين (30%) → الإعدادات (النوع، النشر، SEO)
	•	استخدم مكونيّ:
	•	<Card> و <Tabs> من shadcn/ui
	•	<Form> مع react-hook-form و zod للتحقق من القيم
	6.	الذكاء الاصطناعي المساعد:
	•	فعّل زر بجانب العنوان باسم “اقتراح من الذكاء الاصطناعي 🤖” يستدعي /api/ai/generate-title.
	•	فعّل نفس المبدأ في العنوان الفرعي.
	•	عند توليد العنوان يتم وضع اقتراحات متعددة في قائمة منسدلة لاختيار الأنسب.

⸻

⚙️ مخرجات التطوير
	•	ملف الصفحة: /app/admin/news/create/page.tsx
	•	الـ Schema: newsCreateSchema داخل /lib/validations/news.ts
	•	الـ API endpoint: /api/news/create
	•	الـ Model المحدث في Prisma:

model News {
  id              String   @id @default(cuid())
  title           String
  subtitle        String?
  content         String
  news_type       String   @default("regular")
  publish_type    String   @default("instant")
  scheduled_at    DateTime?
  meta_title      String?
  meta_description String?
  slug            String?  @unique
  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt
}



⸻

✅ المخرجات المتوقعة

صفحة إنشاء خبر ديناميكية، منظمة، وسهلة الاستخدام تتكامل مع الذكاء الاصطناعي وتوفر إعدادات النشر وSEO في واجهة واحدة.

⸻

هل ترغب يا علي أن أجهز لك أيضًا واجهة التصميم الكاملة بالـ React + Tailwind (الكود الفعلي) لهذه الصفحة؟