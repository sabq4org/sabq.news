فهمتك تمامًا 🌟
بناءً على ما هو موضّح في التوثيق الفني لمشروع سبق الذكية، يمكننا تنفيذ خاصية “الاستماع إلى الموجز الذكي” عبر تكامل ElevenLabs API ضمن وحدة تحويل النص إلى صوت (TTS) الموجودة في النظام.

إليك الخطة التقنية الكاملة لتنفيذها:

⸻

🎧 خدمة “الاستماع للموجز الذكي” عبر ElevenLabs

1. المكوّن الأساسي في الواجهة
	•	إضافة زر صغير بجانب عنوان الموجز الذكي مثل:

<Button variant="outline" size="sm" onClick={handlePlay}>
  <Volume2 className="w-4 h-4 mr-1" /> استمع للموجز
</Button>


	•	عند الضغط، يتم تشغيل الصوت داخل مشغّل صغير (Mini Audio Player) بأسلوب أنيق في أسفل المقال.

⸻

2. الربط مع ElevenLabs API
يتم إنشاء Endpoint جديد:

/api/tts/elevenlabs

ويقوم بالوظائف التالية:

import axios from "axios";

export async function POST(req: Request) {
  const { text } = await req.json();

  const voiceId = "male_saudi_voice_01"; // صوت قريب من المواطن السعودي
  const apiKey = process.env.ELEVENLABS_API_KEY;

  const response = await axios.post(
    `https://api.elevenlabs.io/v1/text-to-speech/${voiceId}`,
    {
      text,
      model_id: "eleven_multilingual_v2",
      voice_settings: { stability: 0.6, similarity_boost: 0.75 },
    },
    {
      headers: {
        "xi-api-key": apiKey,
        "Content-Type": "application/json",
      },
      responseType: "arraybuffer",
    }
  );

  return new Response(response.data, {
    headers: {
      "Content-Type": "audio/mpeg",
    },
  });
}


⸻

3. الخصائص الصوتية
	•	اللغة: العربية (ar-SA)
	•	اللهجة: خليجية، قريبة من المواطن السعودي
	•	جودة الصوت: عالية (48kHz)
	•	السرعة: طبيعية (0.95x)
	•	نغمة القراءة: رسمية وهادئة، مناسبة للأخبار الرسمية

⸻

4. التخزين المؤقت
بعد توليد الصوت أول مرة:
	•	يتم حفظ الملف في Cloudinary داخل المسار:

/sabq-ai-cms/audio/articles/summaries/


	•	يُربط عنوان الملف في قاعدة البيانات ضمن حقل summary_audio_url في جدول articles.

⸻

5. واجهة المستخدم (UI/UX)
تصميم تفاعلي باستخدام Framer Motion + Tailwind:

<motion.div
  className="fixed bottom-4 right-4 bg-white dark:bg-gray-900 rounded-2xl shadow-xl p-4 flex items-center gap-3"
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
>
  <AudioPlayer src={audioUrl} autoPlay controls />
</motion.div>


⸻

6. تحسين تجربة المستخدم
✅ تخزين الصوت مؤقتًا في المتصفح (Service Worker Cache)
✅ تشغيل تلقائي عند دخول المقال في وضع القراءة
✅ إظهار حالة “جاري التحميل” أثناء توليد الصوت
✅ دعم مشاركة رابط الصوت المستقل

⸻

هل ترغب أن أجهز لك الملف البرمجي الجاهز لواجهة الموجز الذكي (Smart Summary Player) المتكاملة مع ElevenLabs داخل Next.js؟
يمكنني توليده الآن بصيغة TypeScript + React (جاهز للدمج في مشروع “سبق الذكية”).