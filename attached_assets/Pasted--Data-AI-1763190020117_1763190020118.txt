# 🔥 المخطط الهندسي الشامل لقسم التحليل العميق المدعوم بالذكاء الاصطناعي
## "سبق الذكية" – Data & AI Intelligence Unit Architecture

---

## 📐 1. الطبقة العليا: نظرة شمولية (High-Level Architecture)

```mermaid
graph TB
    subgraph "مصادر البيانات (Data Sources Layer)"
        A1[مستخدمون - Next.js Frontend]
        A2[تطبيقات الموبايل - React Native]
        A3[النظام الأساسي - Supabase CMS]
        A4[منصات التواصل الاجتماعي - APIs]
        A5[محركات البحث - SEO Logs]
        A6[التحليلات الخارجية - SimilarWeb]
    end

    subgraph "طبقة الالتقاط والتدفق (Ingestion & Streaming Layer)"
        B1[Fluentd / Vector<br/>Log Collection]
        B2[Apache Kafka<br/>Streaming Queue]
        B3[Webhook Gateway<br/>Secure API Endpoints]
        B4[CDC Tool<br/>Debezium for PostgreSQL]
    end

    subgraph "منصة البيانات (Data Platform Layer)"
        C1[Data Lake<br/>AWS S3 + Parquet Format]
        C2[Data Warehouse<br/>Snowflake / BigQuery]
        C3[Operational DB<br/>PostgreSQL (Supabase)]
        C4[Cache & Feature Store<br/>Redis Cluster]
    end

    subgraph "طبقة هندسة البيانات (Data Engineering Layer)"
        D1[Apache Airflow<br/>Orchestration]
        D2[dbt Core<br/>Transformations]
        D3[Great Expectations<br/>Data Quality]
        D4[Spark for PySpark<br/>Large Scale Processing]
    end

    subgraph "مصنع النماذج (AI Model Factory Layer)"
        E1[MLflow Tracking<br/>Experiments]
        E2[Model Registry<br/>Version Control]
        E3[Training Cluster<br/>GPU A100 x4]
        E4[Feature Store<br/>Feast on Redis]
    end

    subgraph "طبقة نشر النماذج (Model Serving Layer)"
        F1[FastAPI Services<br/>Async Model Serving]
        F2[ONNX Runtime<br/>Optimized Inference]
        F3[OpenAI API Gateway<br/>GPT-4o Integration]
        F4[HuggingFace Inference<br/>Arabic Models]
    end

    subgraph "طبقة التطبيقات (Application Layer)"
        G1[Smart Editor<br/>Next.js Component]
        G2[Analytics Dashboard<br/>Metabase + Custom UI]
        G3[Recommendation Engine<br/>Real-time API]
        G4[Deep Analysis Engine<br/>Auto-Insights]
        G5[AI Governance Panel<br/>Bias Monitoring]
    end

    subgraph "المراقبة والحوكمة (Monitoring & Governance)"
        H1[Prometheus + Grafana<br/>Infrastructure]
        H2[Weights & Biases<br/>Model Performance]
        H3[Sentry<br/>Error Tracking]
        H4[Datadog<br/>End-to-End Tracing]
    end

    subgraph "الأمان والامتثال (Security & Compliance)"
        I1[HashiCorp Vault<br/>Secrets Management]
        I2[Cloudflare WAF<br/>DDoS Protection]
        I3[Row-Level Security<br/>PostgreSQL RLS]
        I4[SOC2 Audit Logs<br/>Compliance]
    end

    %% Data Flow Connections
    A1 --> B1
    A2 --> B1
    A3 --> B4
    A3 --> B3
    A4 --> B3
    A5 --> B1
    A6 --> B3
    
    B1 --> C1
    B2 --> C1
    B3 --> C3
    B4 --> C1
    
    C1 --> D1
    C3 --> D1
    D1 --> D2
    D2 --> C2
    D2 --> C4
    
    C2 --> E3
    C4 --> E4
    
    E3 --> E1
    E1 --> E2
    
    E2 --> F1
    E4 --> F1
    F3 --> F1
    
    F1 --> G1
    F1 --> G2
    F1 --> G3
    F1 --> G4
    F1 --> G5
    
    G1 --> H4
    F1 --> H2
    D1 --> H1
    C3 --> H3
    
    I1 -.-> D1
    I1 -.-> F1
    I1 -.-> C3
    I2 -.-> B3
    I3 -.-> C3
    I4 -.-> C1
```

---

## 🔧 2. تفاصيل كل مكوّن (Component Deep Dive)

### **A. مصادر البيانات (Data Sources)**

| المصدر | التقنية | الحجم اليومي | التردد | التفاصيل التقنية |
|--------|---------|--------------|--------|------------------|
| **سلوك المستخدمين** | Next.js Analytics API | 50GB/يوم | كل 5 دقائق | إرسال events عبر `navigator.sendBeacon()` |
| **قاعدة البيانات الأساسية** | Supabase PostgreSQL | 2TB إجمالي | real-time | Change Data Capture باستخدام Debezium |
| **التواصل الاجتماعي** | Python Scrapers | 5GB/يوم | كل 15 دقيقة | APIs: Twitter v2, Facebook Graph, Instagram Basic |
| **سجلات الخادم** | Fluentd | 20GB/يوم | streaming | جمع logs من Vercel, AWS CloudWatch |
| **محركات البحث** | Google Search Console API | 500MB/يوم | يوميًا | جلب كلمات البحث والـ CTR |

---

### **B. طبقة الالتقاط (Ingestion Layer) - التنفيذ العملي**

```yaml
# fluentd.conf - جمع سجلات المستخدمين
<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<filter user_behavior.**>
  @type record_transformer
  enable_ruby true
  <record>
    normalized_arabic ${record["title"].to_s.unicode_normalize(:nfc)}
    timestamp ${Time.now.to_i}
  </record>
</filter>

<match user_behavior.**>
  @type kafka2
  brokers kafka-cluster:9092
  topic user-events
  format json
</match>
```

```python
# Debezium CDC Configuration - PostgreSQL to Kafka
{
  "name": "supabase-cdc-connector",
  "config": {
    "connector.class": "io.debezium.connector.postgresql.PostgresConnector",
    "database.hostname": "db.supabase.co",
    "database.port": "5432",
    "database.user": "${ vault('supabase_user') }",
    "database.password": "${ vault('supabase_pass') }",
    "database.dbname": "postgres",
    "table.include.list": "public.articles,public.users,public.interactions",
    "topic.prefix": "supabase",
    "poll.interval.ms": 5000
  }
}
```

---

### **C. منصة البيانات (Data Platform) - البنية الفعلية**

```sql
-- هوية البيانات في Snowflake (المستقبلي)
CREATE SCHEMA news_analytics;
CREATE TABLE fact_article_performance (
    article_id STRING PRIMARY KEY,
    views INT,
    read_time_seconds INT,
    scroll_depth_percent DECIMAL(5,2),
    publish_timestamp TIMESTAMP,
    author_id STRING,
    cluster_key (publish_timestamp, author_id)
);

-- التخزين المؤقت في Redis للميزات
# Pipeline لحفظ بيانات المستخدم
HMSET user:12345:session:6789 \
  article_id "art_987" \
  read_time "145" \
  scroll_depth "0.85" \
  timestamp "1699999999"

# Feature Store for ML
ZADD trending_articles:today 95.5 "art_456"
```

---

### **D. هندسة البيانات (Data Engineering) - بايبلاين Airflow**

```python
# /dags/article_processing.py
from airflow import DAG
from airflow.providers.postgres.operators.postgres import PostgresOperator
from airflow.providers.snowflake.transfers.postgres_to_snowflake import PostgresToSnowflakeOperator

with DAG('article_ml_pipeline', schedule='*/5 * * * *') as dag:
    
    # 1. استخراج بيانات المقالات الجديدة
    extract_new = PostgresOperator(
        task_id='extract_new_articles',
        sql="""
            SELECT * FROM articles 
            WHERE created_at > {{ ti.xcom_pull(task_ids='get_last_run') }}
        """
    )
    
    # 2. تحويل النص العربي
    transform_arabic = PythonOperator(
        task_id='normalize_arabic_text',
        python_callable=normalize_arabic_nlp,
        op_kwargs={'input_table': 'raw_articles'}
    )
    
    # 3. تحميل إلى Feature Store
    load_features = PythonOperator(
        task_id='load_to_redis',
        python_callable=load_features_to_redis
    )
    
    # 4. التحقق من الجودة
    validate = GreatExpectationsOperator(
        task_id='validate_data_quality',
        data_context_root_dir='/great_expectations'
    )
    
    extract_new >> transform_arabic >> load_features >> validate
```

---

### **E. مصنع النماذج (Model Factory) - تدريب GPT-4o على المحتوى العربي**

```python
# /models/trainer/article_performance_predictor.py
import mlflow
import pandas as pd
from transformers import AutoTokenizer, AutoModelForSequenceClassification
from sklearn.model_selection import train_test_split

# التسجيل في MLflow
mlflow.set_tracking_uri("http://mlflow.sabq.ai:5000")
mlflow.set_experiment("article_performance")

# تحميل البيانات
df = spark.read.parquet("s3://sabq-datalake/article-features/").toPandas()

# النموذج العربي المخصص
tokenizer = AutoTokenizer.from_pretrained("asafaya/bert-base-arabic")
model = AutoModelForSequenceClassification.from_pretrained(
    "asafaya/bert-base-arabic",
    num_labels=1  # توقع رقمي (views)
)

# Fine-tuning
with mlflow.start_run():
    # تسجيل parameters
    mlflow.log_param("model", "bert-base-arabic")
    mlflow.log_param("epochs", 5)
    
    # التدريب
    # ... (كود التدريب هنا)
    
    # تسجيل النموذج
    mlflow.pytorch.log_model(model, "model")
    
    # تسجيل metrics
    mlflow.log_metric("mae", 0.234)
```

---

### **F. نشر النماذج (Model Serving) - FastAPI optimized**

```python
# /api/v1/services/prediction_service.py
from fastapi import FastAPI, Depends
from pydantic import BaseModel
import onnxruntime as ort
import redis

app = FastAPI(title="Sabq AI Prediction Service")

# تحميل النموذج المحسّن
sess = ort.InferenceSession("model.onnx")
redis_client = redis.Redis(cluster=True, hosts=[("cache.sabq.ai", 6379)])

class ArticleInput(BaseModel):
    title: str
    content: str
    category: str
    author_id: str

@app.post("/predict/performance")
async def predict_performance(article: ArticleInput):
    # استرجاع الميزات من Redis
    author_avg = redis_client.hget(f"author:{article.author_id}:stats", "avg_views")
    
    # معالجة النص
    features = prepare_features(article.title, article.content, author_avg)
    
    # inference (أقل من 50ms)
    prediction = sess.run(None, {"input": features})[0]
    
    return {
        "predicted_views": int(prediction * 1000),
        "confidence": 0.87,
        "optimization_tips": generate_tips(article)
    }

# Authentication
from fastapi.security import HTTPBearer
security = HTTPBearer()

@app.middleware("http")
async def verify_token(request, call_next):
    token = request.headers.get("Authorization")
    if not verify_sabq_token(token):
        raise HTTPException(status_code=403)
    return await call_next(request)
```

---

### **G. التكامل مع Next.js (Smart Editor)**

```typescript
// /components/SmartEditor/AIInsightsPanel.tsx
import { useQuery } from '@tanstack/react-query';
import { sabqAI } from '@/lib/ai-client';

interface AIInsightsProps {
  articleId: string;
  content: string;
}

export function AIInsightsPanel({ articleId, content }: AIInsightsProps) {
  const { data: insights, isLoading } = useQuery({
    queryKey: ['ai-prediction', articleId],
    queryFn: async () => {
      const response = await sabqAI.post('/predict/performance', {
        title: content.slice(0, 100),
        content,
        category: 'politics',
        author_id: 'auth_123'
      });
      return response.data;
    },
    staleTime: 1000 * 60 * 5, // 5 دقائق
  });

  if (isLoading) return <SkeletonLoader />;

  return (
    <div className="bg-slate-900/50 backdrop-blur p-4 rounded-lg border border-cyan-500/30">
      <div className="flex items-center gap-2 mb-3">
        <Brain className="w-5 h-5 text-cyan-400" />
        <h3 className="text-cyan-300 font-bold">تحليل الذكاء الاصطناعي</h3>
      </div>
      
      <MetricCard 
        label="المشاهدات المتوقعة" 
        value={insights.predicted_views.toLocaleString()}
        icon={<TrendingUp className="text-green-400" />}
      />
      
      <OptimizationTips tips={insights.optimization_tips} />
      
      <BiasWarning score={insights.bias_score} />
    </div>
  );
}
```

---

## 🔐 3. طبقة الأمان والحوكمة (Security Layer)

```yaml
# HashiCorp Vault Policy for AI Team
path "secret/data/sabq/ai/*" {
  capabilities = ["read"]
  allowed_parameters = {
    "*" = []
  }
}

path "secret/data/sabq/database/*" {
  capabilities = ["read"]
  allowed_users = ["data-engineer@supabase.ai", "ml-engineer@supabase.ai"]
}

# Row-Level Security في Supabase
CREATE POLICY "محررون يشاهدون بياناتهم فقط" ON articles
    FOR SELECT
    USING (auth.uid() = author_id);

CREATE POLICY "محللو AI يصلون إلى البيانات المجهولة" ON user_behavior
    FOR SELECT
    USING (auth.role() = 'ai_analyst');
```

---

## 📊 4. لوحة مراقبة الأداء (Observability Dashboard)

```mermaid
graph LR
    subgraph "Infrastructure Metrics"
        I1[CPU Usage < 70%]
        I2[Memory < 80%]
        I3[API Latency p99 < 200ms]
    end
    
    subgraph "Data Quality Metrics"
        D1[Data Freshness < 5min]
        D2[Null Values < 1%]
        D3[Arabic Text Normalization Rate = 100%]
    end
    
    subgraph "Model Metrics"
        M1[Prediction Accuracy > 85%]
        M2[Model Drift < 0.05]
        M3[Bias Score < 0.1]
    end
    
    subgraph "Business KPIs"
        B1[Article Engagement +25%]
        B2[Editor Adoption Rate > 90%]
        B3[AI-driven Recommendations CTR > 12%]
    end
```

---

## 💰 5. تكلفة البنية التقريبية (Cost Estimation - السنة الأولى)

| المكوّن | التقنية | التكلفة الشهرية | ملاحظات |
|----------|---------|----------------|---------|
| **البنية التحتية** | AWS / GCP | $3,500 | EC2, S3, Redis Managed |
| **قاعدة البيانات** | Snowflake | $2,000 | 2TB storage + compute |
| **التدريب** | GPU A100 x4 (AWS) | $8,000 | تشغيل 8 ساعات/يوم |
| **أدوات المراقبة** | Datadog + W&B | $1,200 | 50 hosts + 5 users |
| **المصادقة** | HashiCorp Vault | $500 | Enterprise license |
| **المجموع الشهري** | | **$15,200** | ≈ 57,000 ريال سعودي |
| **المجموع السنوي** | | **$182,400** | ≈ 684,000 ريال سعودي |

---

## 🎯 6. خارطة الطريق التنفيذية (90-Day MVP)

### **الأسبوع 1-2: البنية التحتية**
- إعداد Kafka + Fluentd
- تهيئة S3 Data Lake
- تثبيت Airflow على Kubernetes

### **الأسبوع 3-4: بايبلاين البيانات**
- بناء أول DAG لاستخراج المقالات
- تنفيذ RLS في Supabase
- إعداد MLflow tracking server

### **الأسبوع 5-6: النموذج الأولي**
- تدريب نموذج BERT العربي لتصنيف المقالات
- تحويله إلى ONNX
- نشره على FastAPI

### **الأسبوع 7-8: التكامل**
- بناء Smart Insights Panel في Next.js
- ربطه بالـ API
- تنفيذ caching مع Redis

### **الأسبوع 9-10: الاختبار والأمان**
- اختبار ضغط للـ API (k6)
- تدقيق أمني (penetration test)
- إعداد سياسات AI Ethics

### **الأسبوع 11-12: الإطلاق والمراقبة**
- إطلاق beta للمحررين
- تتبع KPIs في Grafana
- جمع feedback

