تمام 👌
هذا البرومبت الجاهز تقدر تستخدمه مباشرة داخل بيئة تطوير “سبق الذكية” لتوليد وحدة مكتبة الوسائط الذكية (Smart Media Library) المدمجة في لوحة التحكم، واللي تجمع بين إدارة الصور اليدوية والاقتراحات التلقائية بالذكاء الاصطناعي 👇

⸻

🎯 برومبت نصي موجه لتطبيق الذكاء الاصطناعي:

بصفتي خبيرًا في بناء لوحات التحكم وتطبيقات إدارة المحتوى (CMS) باستخدام Next.js وTypeScript وTailwind CSS، أريد منك تنفيذ وحدة مكتبة وسائط (Media Library) متكاملة داخل لوحة التحكم الخاصة بمشروع “سبق الذكية”.

🧱 الهدف:
إضافة مكتبة وسائط احترافية تُدمج مع خاصية رفع الصور عند نشر المقال، بحيث:
	1.	كل صورة يتم رفعها لأي مقال تُحفظ تلقائيًا داخل مكتبة الوسائط العامة.
	2.	يمكن للمحرر عند رفع صورة جديدة أن يختار بين:
	•	رفع صورة من جهازه الشخصي.
	•	أو اختيار صورة موجودة مسبقًا من مكتبة الوسائط.

⸻

🗂️ المزايا المطلوبة:
	1.	عرض المكتبة في واجهة مستقلة داخل لوحة التحكم /admin/media-library
	•	واجهة شبكية (Grid View) + قائمة جانبية للفولدرات.
	•	بحث فوري (Search) بالكلمة المفتاحية، أو اسم الملف، أو الوسوم.
	•	دعم الفلترة حسب الفئة (مقالات – شعارات – صور المراسلين – بانرات).
	•	خاصية السحب والإفلات لإعادة ترتيب الصور داخل المجلدات.
	2.	دعم إدارة المجلدات (Folders Management):
	•	إنشاء مجلد جديد.
	•	إعادة تسمية المجلد.
	•	نقل صور بين المجلدات.
	•	حذف مجلد بالكامل (مع تأكيد أمني).
	3.	خصائص الصورة الواحدة (Image Metadata):
	•	الاسم والعنوان والوصف.
	•	الكلمات المفتاحية (Tags).
	•	خيار “إضافة إلى المفضلة” (Favorite).
	•	إظهار المقالات أو الكيانات التي استخدمت الصورة سابقًا.
	4.	التكامل مع الذكاء الاصطناعي:
	•	عند رفع صورة جديدة، يقوم النظام بتحليل عنوان المقال والمحتوى ليقترح صورًا مشابهة من المكتبة (entity matching).
	•	يمكن للنظام البحث تلقائيًا في الأرشيف بناءً على الكيانات المستخرجة من العنوان مثل:
	•	الملك سلمان بن عبدالعزيز → صور مجلس الوزراء.
	•	وزارة الإعلام → صور الشعار أو الاجتماعات.
	•	اقتراح تلقائي للوسوم المناسبة للصورة باستخدام تحليل السياق.
	5.	التكامل مع نظام التخزين السحابي الحالي:
	•	استخدام Cloudinary كمخزن أساسي.
	•	حفظ روابط الملفات ومعلوماتها في جدول media_files داخل PostgreSQL.
	•	دعم عرض الصور المصغرة (Thumbnails) والتحسين التلقائي.
	6.	واجهة البحث الذكية داخل المحرر:
	•	عند فتح مربع رفع الصور داخل صفحة تحرير المقال، يظهر خيار:
	•	🔍 “ابحث في مكتبة الوسائط”
	•	البحث يعرض نتائج مصغّرة مع إمكانية التصفية أو الاختيار المباشر.

⸻

🧩 التصميم (UI/UX):
	•	يعتمد على Tailwind CSS + Radix UI + Framer Motion.
	•	تصميم عصري بلون فاتح متوافق مع ثيم لوحة التحكم.
	•	عرض صور بشكل بطاقات مربعة متجاوبة (Responsive Grid).
	•	نافذة منبثقة (Modal) للمعاينة والتحرير Metadata.

⸻

🗃️ قاعدة البيانات المقترحة:

media_files {
  id: String PRIMARY KEY
  file_name: String
  folder_id: String (FK → media_folders.id)
  url: String
  type: String
  size: Integer
  width: Integer
  height: Integer
  keywords: String[]
  is_favorite: Boolean DEFAULT false
  used_in: String[]        -- روابط المقالات أو الكيانات التي استخدمت الصورة
  uploaded_by: String (FK → users.id)
  created_at: TIMESTAMP DEFAULT NOW()
}

media_folders {
  id: String PRIMARY KEY
  name: String
  parent_id: String?
  created_at: TIMESTAMP DEFAULT NOW()
}


⸻

⚙️ واجهات API المطلوبة:
	•	GET /api/media — عرض جميع الصور مع البحث والفلاتر.
	•	POST /api/media/upload — رفع صورة جديدة إلى Cloudinary + تخزين Metadata.
	•	PUT /api/media/:id — تعديل بيانات الصورة (العنوان، الكلمات المفتاحية، المجلد…).
	•	DELETE /api/media/:id — حذف صورة.
	•	POST /api/media/folders — إنشاء مجلد جديد.
	•	GET /api/media/suggestions — اقتراح صور بناء على العنوان والمحتوى (مدعوم بـ GPT-4o).

⸻

💡 ميزات إضافية مقترحة:
	•	حفظ سجل الاستخدام (Usage Log) لكل صورة.
	•	خاصية التصنيف التلقائي (Auto Categorization) بالذكاء الاصطناعي.
	•	إنشاء مجلد خاص “المفضلة” يظهر دائمًا أعلى الشجرة.
	•	دعم خاصية Drag-to-Upload المباشر على مجلد معين.

⸻

هل ترغب أزوده الآن بنموذج UI تخيّلي (Mockup) للواجهة مع التخطيط الشبكي والأزرار والـ Sidebar الجانبي؟
أقدر أجهزه بصيغة Figma-ready أو كـ Tailwind component structure حسب اختيارك.