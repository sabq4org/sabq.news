تمام يا علي 👌
اللي فهمته من كلامك ومن الصورة إنك تبي وحدة “الموضوعات الشائعة” أو “الكلمات المفتاحية الأكثر تداولًا” تظهر بشكل أنيق، بحيث المستخدم لما يضغط على كلمة مفتاحية، يعرض له كل الأخبار أو المقالات المرتبطة بها، بدل ما تكون مجرد تصنيفات جامدة.

خلني أعطيك برومبت احترافي تقدر توجهه للذكاء الاصطناعي (في محرر الـAI داخل مشروع “سبق الذكية”) لبناء هذا البلوك الذكي:

⸻

🧠 برومبت إنشاء وحدة الكلمات المفتاحية الذكية (Trending Keywords Block)

البرومبت (جاهز للنسخ):

أنشئ مكونًا ذكيًا في واجهة مشروع سبق الذكية لعرض الكلمات المفتاحية الأكثر تداولًا خلال آخر 72 ساعة من قاعدة البيانات (جدول smart_terms أو articles.tags إن وُجد).
يجب أن:
	1.	يُعرض المكون بتصميم عصري بلون داكن أو فاتح تلقائي حسب السمة العامة للنظام.
	2.	يحتوي على عناوين دائرية أو كبسولات (tags) بشكل أنيق مثل “chip components”، مع ألوان مختلفة لكل كلمة حسب تصنيفها (سياسي، اقتصادي، رياضي، اجتماعي…).
	3.	عند الضغط على أي كلمة مفتاحية:
	•	يتم تنفيذ فلترة فورية للأخبار عبر API /api/articles?tag={keyword}.
	•	يتم تفعيل تأثير متحرك (Framer Motion) لعرض النتائج الجديدة بسلاسة.
	4.	الكلمات تُرتب تنازليًا حسب معدل التفاعل usage_count أو views_count.
	5.	عند عدم وجود بيانات، يظهر نص تفاعلي:
“لا توجد كلمات متداولة حاليًا — تابع آخر الأخبار لمعرفة ما يشغل القراء 🔍”
	6.	إضافة شريط تحديث تلقائي كل 10 دقائق لجلب أحدث الكلمات.
	7.	تصميم المكون باستخدام:
	•	Next.js 15
	•	TypeScript
	•	Tailwind CSS
	•	Framer Motion
	•	Radix UI (Badge أو Chip)
	8.	دعم الاتجاه العربي RTL كامل، مع اختبار الواجهة في الوضعين (Dark / Light).

⸻

✨ اقتراح تصميم الواجهة (UI)

<div className="flex flex-wrap gap-2 p-4 rounded-2xl bg-muted/40 backdrop-blur-sm">
  {trendingKeywords.map((tag, i) => (
    <motion.button
      key={i}
      whileHover={{ scale: 1.05 }}
      whileTap={{ scale: 0.95 }}
      onClick={() => handleKeywordClick(tag.name)}
      className={`px-3 py-1.5 rounded-full text-sm font-medium text-white transition
        ${tag.category === "politics" ? "bg-red-600" :
          tag.category === "economy" ? "bg-green-600" :
          tag.category === "sports" ? "bg-blue-600" :
          "bg-gray-600"}`}
    >
      #{tag.name}
    </motion.button>
  ))}
</div>


⸻

هل تبي أجهز لك النسخة الكاملة من المكون (React functional component) بحيث تكون جاهزة للنسخ في /components/TrendingKeywords.tsx؟
أقدر أخليها متصلة مباشرة بـ smart_terms API مع التصميم النهائي Tailwind + Framer Motion.