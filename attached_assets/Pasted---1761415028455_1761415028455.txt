أبو محمد 🌟
جهّزت لك “برومبت” تنفيذي متكامل لإعداد خدمة النشرات الإخبارية الصوتية في لوحة التحكم، مع دمج ElevenLabs (صوت عربي واقعي سعودي، اختيار الصوت/الاسم/حدة النبرة)، وإشارة في الصفحة الرئيسية، وأرشيف، وبلوك واجهة يدل أنها AI—بالإضافة إلى إضافات ذكية تخدم العمل التحريري.

ملاحظة أمان مهمة: وصلتني مفاتيح حساسة. لا نحتفظ بها ولا نعرضها. رجاءً استخدم متغيرات بيئة وفعّل التدوير الدوري للمفاتيح. استبدل أي مفاتيح مكشوفة فورًا.

برومبت إنشاء خدمة “النشرة الصوتية” (للـ Agent / Dev Assistant)

الدور/الهدف:
أنشئ خدمة “النشرة الصوتية” داخل Sabq AI CMS (Next.js 15 + TypeScript + Tailwind + Radix UI + Framer Motion). الخدمة تعتمد ElevenLabs TTS لتوليد نشرات صوتية عربية سعودية واقعية من نصوص إخبارية أو ملخصات AI، مع أرشيف، وإشارة حيّة في الواجهة، وبلوك مخصص في الموقع.

⸻

1) نطاق العمل والميزات
	1.	لوحة تحكم /admin/audio-newsletters
	•	إنشاء نشرة صوتية جديدة: عنوان، ملخص، نص النشرة، كلمات مفتاحية، قسم/تصنيف اختياري.
	•	اختيار الصوت من قائمة أصوات ElevenLabs (اجلب الأصوات عبر API):
	•	language يجب أن يدعم ar، وaccent سعودي قدر الإمكان (أو أقرب بديل).
	•	خصائص قابلة للتعديل: pitch (حدة النبرة)، stability, similarity_boost, style, speaking_rate.
	•	إعدادات الصوت الإضافية:
	•	Intro/Outro قابلان للتحرير (جملة افتتاحية/ختامية ثابتة).
	•	Bed Music اختياري (MP3 قصير منخفض الصوت).
	•	Normalization & Loudness تلقائي (LUFS -16 للبث على الويب).
	•	التحويل لنص→صوت (سيرفر سايد) مع معاينة قبل الحفظ.
	•	جدولة النشر بتاريخ/وقت؛ حالة: draft → ready → published.
	•	رفع الأصول الصوتية النهائية إلى Cloudinary/S3 بمسار منظم.
	•	نسخة نصية/تفريغ تحفظ تلقائياً (للـ SEO وللبحث الداخلي).
	•	وسوم AI: وسم المحتوى بأنه “مولّد بالذكاء الاصطناعي” مع نسبة الاعتماد (مثلاً 70% AI / 30% تحرير).
	2.	الأرشيف العام /audio-newsletters
	•	قائمة قابلة للبحث والترشيح (حسب التاريخ، الكلمات المفتاحية، التصنيف، مدة الصوت).
	•	صفحة تفاصيل النشرة: مشغّل صوت، ملخص، نص النشرة، الوسوم، مشاركة اجتماعية، ووسم AI واضح.
	•	تنزيل MP3، ومشاركة كرابط موجّه يبدأ من Timestamp محدد.
	3.	إشارة في الصفحة الرئيسية
	•	Badge/Indicator حيّ: “🔴 النشرة الصوتية جاهزة/قيد التسجيل” (pulse animation) يظهر عند وجود نشرة Scheduled خلال 60 دقيقة أو Published خلال آخر 3 ساعات.
	•	عند الضغط: يفتح أحدث نشرة في مشغّل منبثق (Modal/Drawer).
	4.	بلوك واجهة “النشرة الصوتية”
	•	مكوّن واجهة (React + Tailwind + Radix) باسم AudioNewsletterBlock قابل للزرع في أي مكان (الصفحة الرئيسية/داخل الأقسام).
	•	يعرض آخر N نشرات (افتراضي 1-3)، مع شارة AI صغيرة (مثلاً “AI-Generated”).
	•	يدعم ثيم فاتح/غامق، وأحجام S/M/L، وخيار Autoplay=off افتراضي.
	•	دعم Waveform بسيط وHotkeys (مسافة للتوقيف/تشغيل).
	•	إظهار المدة وتاريخ النشر والتصنيف.
	5.	التوزيع والتكامل
	•	RSS/Podcast Feed: /feeds/audio-newsletters.xml (iTunes-compatible) + JSON Feed.
	•	OG/audio cards مخصصة لمشاركة جميلة على X/WhatsApp.
	•	Analytics Events: audio_play, audio_complete, share_audio, download_audio.
	6.	الأذونات
	•	editor: إنشاء/تحرير/معاينة.
	•	admin: نشر/حذف/إعدادات أصوات/المفاتيح.
	•	reporter: اقتراح نص فقط (بدون تحويل صوتي مباشر).

⸻

2) إعدادات البيئة والأمان
	•	لا تعرض المفاتيح في الواجهة. خزّنها كمتغيرات بيئة على السيرفر:

ELEVENLABS_API_KEY=******** (ضعها من الإعدادات الآمنة)
ELEVENLABS_BASE=https://api.elevenlabs.io
AUDIO_BUCKET_PATH=/sabq-ai-cms/audio/newsletters

	•	فعّل rate limiting لنقاط TTS والتخزين المؤقت (caching) لاستجابات “قائمة الأصوات”.
	•	سجّل كل عملية توليد بصمة (log) مع user_id ولأغراض التدقيق.

⸻

3) نقاط API (تصميم)
	•	GET /api/audio/voices
يجلب قائمة أصوات ElevenLabs (تُخزّن 12 ساعة في Cache)، ويمرّر فقط metadata اللازم: voice_id, name, labels(language, gender, accent), preview_url.
	•	POST /api/audio/synthesize
Body: { text, voice_id, options: { pitch, stability, similarity_boost, style, speaking_rate, intro, outro, music_asset_id? } }
Result: { asset_id, duration, waveform?, url } (ترفع الملف لـ Cloudinary/S3 وتُرجع المسار العام والميتا).
	•	POST /api/audio-newsletters (إنشاء/حفظ مسودة)
	•	PUT /api/audio-newsletters/:id (تحديث)
	•	POST /api/audio-newsletters/:id/publish (نشر وتوليد الـ RSS/JSON تلقائيًا)
	•	GET /api/audio-newsletters (قائمة مع تصفية وترقيم)
	•	GET /api/audio-newsletters/:slug (التفاصيل)
	•	GET /feeds/audio-newsletters.xml و/feeds/audio-newsletters.json

ملاحظات تنفيذ TTS:
	•	استدعِ ElevenLabs (TTS) بسيرفر سايد فقط.
	•	وحّد العيّنة 44.1kHz/16-bit، MP3 128kbps افتراضيًا (مع دعم 192kbps اختياري).
	•	نفّذ fade-in/out بسيط وloudness normalization قبل الرفع.

⸻

4) مخطط قاعدة البيانات (Prisma/Drizzle)

-- جدول النشرات الصوتية
audio_newsletters (
  id uuid pk,
  slug text unique,
  title text not null,
  summary text,
  body text,                   -- نص النشرة النهائي
  keywords text[],             -- كلمات مفتاحية
  category_id int null,
  status text check(status in ('draft','ready','published')) default 'draft',
  ai_ratio int default 70,     -- نسبة الاعتماد على AI
  duration_seconds int,
  voice_id text,               -- من ElevenLabs
  voice_name text,
  tts_options jsonb,           -- pitch, stability, rate...
  audio_asset_id uuid,
  published_at timestamptz,
  created_by uuid references users(id),
  created_at timestamptz default now(),
  updated_at timestamptz default now()
);

-- الأصول الصوتية
audio_assets (
  id uuid pk,
  storage_provider text check(storage_provider in ('cloudinary','s3')),
  path text not null,          -- /sabq-ai-cms/audio/newsletters/2025/10/...
  url text not null,
  size_bytes int,
  duration_seconds int,
  waveform jsonb,
  checksum text,
  created_at timestamptz default now()
);

-- إعدادات الخدمة
audio_settings (
  id serial pk,
  default_voice_id text,
  default_voice_name text,
  default_bitrate int default 128,
  default_style jsonb,         -- pitch/rate/stability defaults
  show_home_badge boolean default true,
  badge_recency_hours int default 3
);

فهارس مقترحة:

create index on audio_newsletters (status, published_at desc);
create index on audio_newsletters using gin (keywords);


⸻

5) مواصفات الواجهة (Admin + Public)

A) لوحة التحكم
	•	صفحة قائمة النشرات: جدول بعمود الحالة/المدة/الصوت/تاريخ + أزرار معاينة/نشر.
	•	صفحة إنشاء/تحرير:
	•	محرر نصي محسّن (RTL) مع قالب نشرة.
	•	شريط جانبي لاختيار الصوت وإعداداته (قائمة الأصوات، المعاينة الصوتية، ضبط pitch/rate).
	•	قسم Intro/Outro وBed Music.
	•	زر توليد الصوت (يستدعي /api/audio/synthesize) مع شريط تقدم.
	•	عرض Waveform + Trim بسيط (بدايات/نهايات).
	•	حفظ كمسودة / جدولة / نشر.

ب) الواجهة العامة
	•	AudioNewsletterBlock: بطاقة واحدة كبيرة أو شبكة 3 بطاقات، كل بطاقة فيها:
	•	مشغّل صوت، عنوان، ملخص، مدة، AI Badge (Tooltip: “تم توليده جزئياً بالذكاء الاصطناعي”).
	•	زر “أرشيف النشرات” يوصل إلى /audio-newsletters.
	•	Indicator أعلى الهيرو: شارة متحركة (Framer Motion pulse) عند توفر نشرات حديثة.

⸻

6) قالب نص النشرة (Prompt داخلي لتوليد نص عربي سعودي)

“حلّل أهم 3–5 أخبار لهذا اليوم المختارة من غرفتنا التحريرية. اكتب نشرة صوتية عربية سعودية بلهجة رسمية خفيفة، واضحة ومختصرة، مدتها 90–180 ثانية.
تتألف من:
	1.	افتتاحية موجزة (جملة ترحيب + عنوان اليوم).
	2.	فقرات الأخبار مرتبة حسب الأهمية، كل فقرة ≤ 30 ثانية، جمل قصيرة قابلة للقراءة الصوتية، أرقام واضحة.
	3.	خاتمة سريعة مع دعوة للمتابعة.
التعليمات:

	•	تجنّب الجُمل الطويلة والأسماء الأجنبية المعقّدة دون تهجئة.
	•	استخدم فواصل لفظية طبيعية، وأشر للمصادر عند الحاجة باقتضاب.
الناتج: نص جاهز للـ TTS بدون ترميز، مع تحديد مواضع الوقفات بنقاط ثلاث (…) عند الحاجة.”

⸻

7) واجهات المطوّر (نماذج مختصرة)

Route: جلب الأصوات

// /app/api/audio/voices/route.ts
export async function GET() {
  const r = await fetch(`${process.env.ELEVENLABS_BASE}/v1/voices`, {
    headers: { 'xi-api-key': process.env.ELEVENLABS_API_KEY! }
  });
  const data = await r.json();
  // صفّ الأصوات العربية/الأقرب للعربية وامنع تمرير مفاتيح حساسة
  const voices = data.voices
    .filter((v:any)=> (v.labels?.language?.startsWith('ar') || v.language?.startsWith?.('ar')))
    .map((v:any)=>({ voice_id: v.voice_id, name: v.name, labels: v.labels, preview_url: v.preview_url }));
  return Response.json({ voices }, { status: 200 });
}

Route: توليد الصوت

// /app/api/audio/synthesize/route.ts
export async function POST(req: Request) {
  const { text, voice_id, options } = await req.json();
  // نداء ElevenLabs TTS (stream → buffer) + معالجة بسيطة + رفع Cloudinary/S3
  // أعِد { asset_id, url, duration, waveform }
  // (التنفيذ الكامل يشمل التحقق، المعدلات، ولوغ التدقيق)
  return Response.json({ asset_id:'...', url:'...', duration:123, waveform:[] });
}


⸻

8) الخواص الذكية المقترحة
	•	Auto-Clipper: قص تلقائي للصمت > 800ms.
	•	Smart Titles: اقتراح عنوان صوتي قصير متوافق مع SEO.
	•	Keyword Hints: توليد كلمات مفتاحية تلقائياً مع إمكانية تعديل.
	•	Chapter Marks: تقسيم النشرة إلى مقاطع بعناوين صغيرة (للاعب يدعم Chapters).
	•	Personalization (لاحقاً): اقتراح نشرات حسب اهتمامات المستخدم.
	•	A/B Testing: مقارنة أداء أصوات مختلفة (ذكر/أنثى/نبرة).

⸻

9) معايير القبول (QA)
	•	الصوت العربي واضح، لا تشويش، لا Clips.
	•	زمن التوليد < 15ث لمعظم النشرات القصيرة.
	•	المدّة، الحجم، وميتا البيانات تظهر في الأرشيف والبلوك.
	•	RSS/JSON تعمل وتتحدّث تلقائياً عند النشر.
	•	شارة AI ظاهرة في البلوك وصفحة التفاصيل.
	•	الأذونات مطبقة حسب الدور.
	•	لا يخرج أي مفتاح/بيانات حساسة للواجهة.

⸻

10) تجربة المستخدم والتصميم
	•	Modern UI، RTL، متجاوب Mobile-First.
	•	Radix UI للمودال/السلايدر، Framer Motion لمؤثرات الـ pulse والـ hover.
	•	Tailwind Utilities: مسافات واضحة، تباين عالٍ، إمكانية التحكم بلوح الألوان.
	•	إمكانية الوصول: اختصارات لوحة مفاتيح، تباين، عناوين بديلة، تركيز واضح.

⸻

ملحوظة أخيرة حول المفاتيح
	•	احفظ المفتاح في البيئة: ELEVENLABS_API_KEY (لا تُلصقه في الكود أو الواجهة).
	•	إن كان المفتاح المعروض قد كُشف للعامة، قم بتدويره فورًا من لوحة ElevenLabs واستبداله.

إذا رغبت، أحوّل هذا البرومبت إلى تذكرة تنفيذ تقنية (Tasks + Subtasks) أو أرفق migrations/ملفات كود جاهزة للنسخ واللصق.